---
phase: 11-polish-continuity
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/app/(public)/how-it-works/page.tsx
  - src/app/(public)/how-it-works/_components/anchor-nav.tsx
  - src/app/(public)/how-it-works/_components/faq.tsx
  - src/components/hero/standings-hero.tsx
  - src/components/hero/landing-hero.tsx
  - src/app/(public)/standings/page.tsx
  - src/app/page.tsx
  - src/components/auth/signin-form.tsx
  - src/components/footer.tsx
  - public/how-it-works/README.md
  - docs/how-it-works-screenshot-runbook.md
  - tests/app/how-it-works.test.tsx
autonomous: false
requirements: [UI-05, UI-01, UI-02]

must_haves:
  truths:
    - "Unauth user hitting /how-it-works sees full explainer page with anchor-nav at top"
    - "How It Works covers all 9 sections in CONTEXT.md with friendly tone and worked examples"
    - "Hero banner renders on /standings and / with PL-purple gradient + wordmark + tagline"
    - "Footer (site-wide) links to /how-it-works"
    - "Signup/signin pages link to /how-it-works for prospective members"
    - "Core mobile flows (predictions, LOS picker, pre-season form, standings, profile) have no horizontal scroll at 375px viewport"
  artifacts:
    - path: "src/app/(public)/how-it-works/page.tsx"
      provides: "Public long-scroll explainer page"
      contains: "How It Works"
    - path: "src/components/hero/standings-hero.tsx"
      provides: "Hero SVG banner for /standings"
      exports: ["StandingsHero"]
    - path: "src/components/hero/landing-hero.tsx"
      provides: "Hero SVG banner for / landing"
      exports: ["LandingHero"]
    - path: "docs/how-it-works-screenshot-runbook.md"
      provides: "Runbook for retaking screenshots when UI changes materially"
      contains: "screenshot"
  key_links:
    - from: "src/app/(public)/how-it-works/page.tsx"
      to: "/public/how-it-works/*.png"
      via: "next/image or img references to committed dev-env screenshots"
      pattern: "how-it-works/.*\\.png"
    - from: "src/components/footer.tsx"
      to: "/how-it-works"
      via: "Next Link in footer navigation"
      pattern: "how-it-works"
---

<objective>
Build the public `/how-it-works` explainer page, drop in-code SVG hero banners on `/standings` and `/`, wire footer + signin-page links, and run a mobile-viewport audit fixing any horizontal-scroll, tap-target, or fixed-footer stacking regressions across the 5 highest-stakes mobile flows.

Purpose: Satisfy UI-05 (public "How It Works" page), complete the UI-01 visual polish layer with hero imagery, and discharge UI-02 (mobile responsiveness) via a focused audit. Runs in Wave 2 alongside Plan 02 — both depend only on Plan 01 (theme tokens + MemberLink).

Output: Full explainer page (9 sections + FAQ + anchor nav), 2 hero components, 5 committed PNG screenshots in /public/how-it-works/, screenshot runbook, mobile audit pass with fixes committed.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-polish-continuity/11-CONTEXT.md
@.planning/phases/11-polish-continuity/11-RESEARCH.md
@.planning/phases/11-polish-continuity/11-01-SUMMARY.md

# Existing patterns and surfaces to audit
@src/app/(public)/standings/page.tsx
@src/app/page.tsx
@src/app/(member)/gameweeks/[gwNumber]/page.tsx
@src/components/predictions/prediction-form.tsx
@src/components/los/los-team-picker.tsx
@src/app/(member)/pre-season/page.tsx
@src/components/footer.tsx

<interfaces>
<!-- Hero SVG pattern — in-code, zero external assets -->

Pattern: Inline SVG hero with CSS gradient (CONTEXT.md locked: no image hosting)
```tsx
export function LandingHero() {
  return (
    <section className="relative bg-gradient-to-br from-pl-purple to-pl-purple-900 text-white py-12 px-6 rounded-b-xl">
      <svg viewBox="0 0 800 120" className="absolute inset-x-0 bottom-0 w-full h-16">
        {/* Stadium silhouette path */}
        <path d="M 0 120 L 50 80 L 100 90 L 200 60 L 400 70 ..." fill="#00ff85" opacity="0.15" />
      </svg>
      <h1 className="text-3xl font-bold">George's Predictor</h1>
      <p className="mt-2 text-pl-green">The 10-year league, now on the web.</p>
    </section>
  )
}
```

Pattern: How It Works sections (CONTEXT.md section list)
1. Welcome  2. How to play  3. Scoring  4. Bonuses  5. Last One Standing  6. H2H Steals  7. Pre-Season Predictions  8. Prizes  9. FAQ

FAQ questions (CONTEXT.md):
- "What happens if a fixture is postponed?"
- "What if there's a tie at the top?"
- "Can I change my prediction after kickoff?"
- "How do I see my past seasons?"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /how-it-works page + anchor-nav + FAQ + screenshot runbook</name>
  <files>
    src/app/(public)/how-it-works/page.tsx,
    src/app/(public)/how-it-works/_components/anchor-nav.tsx,
    src/app/(public)/how-it-works/_components/faq.tsx,
    public/how-it-works/README.md,
    docs/how-it-works-screenshot-runbook.md,
    tests/app/how-it-works.test.tsx
  </files>
  <behavior>
    - Unauth user can load /how-it-works — no redirect to /signin.
    - Page has a sticky (or top-of-page) anchor-nav with 9 jump-links: #welcome, #how-to-play, #scoring, #bonuses, #last-one-standing, #h2h-steals, #pre-season, #prizes, #faq.
    - Each section has a matching id attribute so anchor navigation works.
    - Scoring section contains the worked example from CONTEXT.md: "Say you predict Arsenal 2-1 Chelsea and the result is Arsenal 3-2 Chelsea — you got the result right, that's 10 points. If you'd predicted 3-2 exactly you'd have scored 30."
    - Bonuses section explains each bonus type + Double Bubble (GW10/20/30) + Golden Glory formula (20 result / 60 score).
    - FAQ component renders 4 expandable question/answer pairs for the questions listed above (use <details><summary> or a controlled accordion — keep it simple and JS-free where possible).
    - Screenshots referenced (via <img src="/how-it-works/prediction-form.png" ...>) but MAY not be committed yet — reference the paths with alt text; task-2 checkpoint can commit the binaries.
    - Test asserts page renders all 9 section h2s and the anchor-nav has 9 jump links.
  </behavior>
  <action>
    1. Write failing test tests/app/how-it-works.test.tsx — mount the page (it's a server component; if rendering sync, use next-router test setup or render via a client-wrapped harness). Assert: 9 section headings present, 9 anchor nav links present, 4 FAQ questions present.
    2. Create src/app/(public)/how-it-works/_components/anchor-nav.tsx — simple component that renders a nav with 9 <a href="#..."> links. Style as sticky top-4 on desktop, horizontal scroll on mobile (overflow-x-auto + whitespace-nowrap).
    3. Create src/app/(public)/how-it-works/_components/faq.tsx — renders 4 <details><summary>Question</summary><p>Answer</p></details> elements. No JS needed, native browser disclosure. Covers:
       - "What happens if a fixture is postponed?" — George can void or reschedule; voided fixtures don't count toward predictions or LOS.
       - "What if there's a tie at the top of the gameweek?" — H2H steal triggers; tied members duke it out the following week.
       - "Can I change my prediction after kickoff?" — No; lockout is enforced at kick-off per fixture (not per gameweek).
       - "How do I see my past seasons?" — Click your name anywhere in the app; your profile shows current-season stats and a history of previous seasons.
    4. Create src/app/(public)/how-it-works/page.tsx — server component, no auth check (public). Import and render:
       - <LandingHero /> at top (or a smaller hero variant)
       - <AnchorNav />
       - 9 section elements with ids matching nav links. Friendly copy per CONTEXT.md.
       - Screenshot references: after relevant sections, show <img src="/how-it-works/prediction-form.png" alt="Prediction form screenshot" className="rounded shadow-lg mx-auto max-w-md" /> and similar for gameweek-results, admin-bonus-panel, los-picker, pre-season-form.
       - <FAQ /> as final section.
    5. Create public/how-it-works/README.md — placeholder explaining expected PNGs (prediction-form.png, gameweek-results.png, admin-bonus-panel.png, los-picker.png, pre-season-form.png), ~800px width, PNG optimised.
    6. Create docs/how-it-works-screenshot-runbook.md — one-page steps: (a) run `npm run dev`, (b) log in as Dave (admin), (c) navigate to each URL listed in the runbook, (d) use DevTools device toolbar at 1280x800, (e) take screenshot with browser dev tools, (f) optimise with pngquant or TinyPNG, (g) commit to /public/how-it-works/ with the exact filenames, (h) verify page renders correctly.
    7. Run tests — expect green once the 9 sections + 4 FAQs + 9 anchor nav links are in place.

    AVOID:
    - Client-side interactivity for FAQ (use native <details>).
    - Auth-gating — page is PUBLIC per CONTEXT.md.
    - Heavy JS/animation libraries.
    - Committing actual PNGs in this task — binary commits happen in Task 2 as part of the build-verification step.
  </action>
  <verify>
    <automated>npm run test:run -- tests/app/how-it-works.test.tsx</automated>
  </verify>
  <done>
    - Page live at /how-it-works with 9 sections, anchor nav, 4 FAQs.
    - Screenshot runbook + public README committed.
    - All test assertions green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Hero SVG components + /standings + / wiring + footer + signin links + PNG capture</name>
  <files>
    src/components/hero/standings-hero.tsx,
    src/components/hero/landing-hero.tsx,
    src/app/(public)/standings/page.tsx,
    src/app/page.tsx,
    src/components/auth/signin-form.tsx,
    src/components/footer.tsx,
    public/how-it-works/prediction-form.png,
    public/how-it-works/gameweek-results.png,
    public/how-it-works/admin-bonus-panel.png,
    public/how-it-works/los-picker.png,
    public/how-it-works/pre-season-form.png
  </files>
  <action>
    1. Create src/components/hero/standings-hero.tsx — pure SVG + CSS gradient, title "League Standings", small tagline "Live Premier League predictor standings" with PL purple background and green accents. viewBox + w-full for responsive scaling. No external assets. Height ~120-160px on desktop, 80-100px on mobile via Tailwind responsive classes.
    2. Create src/components/hero/landing-hero.tsx — larger banner for /, title "George's Predictor" with wordmark styling, tagline "Weekly Premier League predictions with your mates", a sign-in CTA button (if unauth) linking to /signin. Include a subtle inline SVG stadium silhouette along the bottom edge (fill with pl-green at 10-20% opacity).
    3. Edit src/app/(public)/standings/page.tsx — import and render <StandingsHero /> at the top of the page, above the existing standings table. Ensure it doesn't break the existing server-component data flow.
    4. Edit src/app/page.tsx — current file is a thin re-export of /standings (per STATE Phase 10 P04). Change to a proper landing page: if unauth, render <LandingHero /> + a compact preview of /standings (top-5 rows) + a "View full standings" CTA. If auth (session exists), retain the re-export to /standings OR redirect to dashboard — match existing auth-flow behaviour (don't change semantics).

       NOTE on end-of-season summary: Plan 04 will add a conditional `/end-of-season` route shown at `/` when the current season is archived. This task should set up `/` so that conditional rendering can be layered on top (e.g. wrap the landing in a query for `seasons.ended_at` and branch on result; Plan 04 can complete the wiring).
    5. Edit src/components/footer.tsx — add a "How it works" link to /how-it-works alongside any existing footer nav. Footer should be visible on public + member + admin layouts.
    6. Edit src/components/auth/signin-form.tsx (or the sign-in page itself) — add a subtle "New here? Learn how it works" link to /how-it-works.
    7. Capture 5 dev-environment screenshots per the runbook:
       - prediction-form.png: /predictions page with 10 fixtures visible (Dave's test account mid-gameweek).
       - gameweek-results.png: /gameweeks/[N] after a completed gameweek with points breakdown.
       - admin-bonus-panel.png: /admin/bonuses page showing bonus-set dialog.
       - los-picker.png: LOS picker UI element from predictions page with several teams available.
       - pre-season-form.png: /pre-season form mid-fill-out with top-4 + relegation + promoted sections visible.
       Commit each as ~800px wide, optimised PNG, to /public/how-it-works/.

       If the executor cannot practically capture screenshots in the CI context, commit 5 placeholder PNGs (solid PL-purple backgrounds 800x600 with the filename overlaid as text) so the page renders without broken-image icons; add a TODO to deferred-items.md noting the screenshots must be retaken per the runbook before launch.
    8. Run `npm run build` to ensure no broken imports, no routing breaks.

    AVOID:
    - Using next/image for the hero (it's in-code SVG, not a raster asset).
    - Hard-coding hex values when pl-purple token exists (use `bg-pl-purple` classes).
    - Changing existing auth redirect logic.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - Hero components live and wired to /standings + /.
    - Footer + signin link to /how-it-works.
    - 5 PNG screenshots (real or placeholder with deferral note) committed.
    - `npm run build` green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mobile audit checkpoint — 5 highest-stakes flows</name>
  <files>N/A — human verification only</files>
  <action>Human verification only — no Claude-authored code. Claude prepares the checkpoint by confirming all Plan 01/02/03 code is on main, the full automated test suite is green, and the build succeeds. Claude also confirms that the proactive mobile-safe patterns listed below have been applied during Plans 01-02 and Task 1-2 of this plan.</action>
  <what-built>
    Plan 01 (theme + MemberLink) and Plan 02 (profile + chart + home widget) and this plan's public /how-it-works + hero banners are all on main. The phase's locked UI-02 requirement is a mobile audit pass.

    Claude has already applied these proactive mobile-safe fixes based on common regressions:
    - Every new component (hero, how-it-works sections, stats-panel, chart) uses Tailwind responsive prefixes (sm:, md:, lg:) with mobile-first breakpoints.
    - Anchor nav on /how-it-works uses overflow-x-auto on narrow viewports.
    - WeeklyPointsChart scales via viewBox + w-full (responsive by design).
    - MemberLink touch target is a full `<a>` (not a sub-span), meets 44px tap-target minimum via padding.

    What remains is pixel-perfect regression against real device emulation that Claude cannot perform meaningfully.
  </what-built>
  <how-to-verify>
    Use Chrome DevTools device emulator (iPhone 13 at 390x844, Pixel 5 at 393x851). Optionally also test on a real phone if accessible.

    Test each of these 5 flows:

    1. **Predictions submit flow** (`/predictions/[gwNumber]`):
       - No horizontal scroll.
       - Score input fields tappable without zoom.
       - Sticky submit bar doesn't cover the last fixture.
       - LOS picker opens without overflow issues.
       - Bonus star selection reachable on each fixture card.

    2. **LOS picker** (same page, dedicated widget):
       - Grid of 20 teams wraps to 2-3 columns on mobile.
       - Team selection has clear visual state.
       - No overflow under keyboard.

    3. **Pre-season form** (`/pre-season`):
       - All 3 sections (top-4, relegated, promoted) usable without horizontal scroll.
       - Championship dropdown fits viewport.
       - Submit button always reachable.

    4. **Standings** (`/standings`):
       - Table scrolls horizontally gracefully (overflow-x-auto on the wrapper) OR column list collapses on mobile (hide less-important columns).
       - Hero banner scales properly.
       - MemberLink tap targets work.

    5. **Member profile** (`/members/[slug]`):
       - Profile header doesn't wrap awkwardly.
       - Stats panel grid collapses to single column on mobile.
       - Weekly-points chart renders and scales (w-full on the <svg>).
       - Season history table readable or horizontally scrollable.

    Report any regressions as:
    - "Flow X has Y issue at Z viewport" — Claude will fix and re-checkpoint.
    - "All flows pass" — merge this to Phase 11 FINAL_QA_CHECKLIST.md §13 as manual sign-off.

    If defects found, Claude commits fixes (usually Tailwind classname changes, flex/grid re-shapes, overflow rules) and re-checkpoints.
  </how-to-verify>
  <verify>Human sign-off after verification of all 5 mobile flows; any defects reported trigger a fix loop then re-checkpoint</verify>
  <done>Human confirms "approved" OR all reported regressions have been fixed and re-verified</done>
  <resume-signal>Type "approved" if all 5 flows pass, or describe issues (flow name + viewport + description) for Claude to fix</resume-signal>
</task>

</tasks>

<verification>
- `/how-it-works` loads unauth, renders 9 sections + 4 FAQs + anchor nav.
- `/standings` and `/` both show hero banners.
- Footer + signin link to /how-it-works.
- 5 screenshots committed (real or placeholder-with-TODO).
- Mobile audit checkpoint approved by user.
- `npm run test:run` green.
- `npm run build` green.
</verification>

<success_criteria>
- UI-05 satisfied: public How It Works page with all 9 sections + 4 FAQs + screenshots.
- UI-01 extended: hero banners on /standings and /.
- UI-02 satisfied: 5 highest-stakes mobile flows pass visual audit at iPhone 13 / Pixel 5 viewports.
- Footer wiring consistent across public, member, admin layouts.
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-continuity/11-03-SUMMARY.md` capturing: screenshots committed (real vs placeholder), mobile fixes applied during audit checkpoint, any deferred items (e.g. screenshot re-shoot scheduled).
</output>
