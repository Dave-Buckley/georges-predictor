---
phase: 11-polish-continuity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/012_polish_continuity.sql
  - src/app/globals.css
  - src/lib/members/slug.ts
  - src/components/shared/member-link.tsx
  - src/components/fixtures/fixture-card.tsx
  - src/components/predictions/predictions-table.tsx
  - src/components/los/admin-los-table.tsx
  - src/components/los/los-standings.tsx
  - src/components/admin/confirm-bonus-awards.tsx
  - src/components/admin/confirm-prize-dialog.tsx
  - src/components/admin/member-table.tsx
  - src/app/(public)/standings/page.tsx
  - src/app/(member)/gameweeks/[gwNumber]/page.tsx
  - src/app/(member)/bonuses/page.tsx
  - src/app/(admin)/admin/bonuses/page.tsx
  - src/app/(admin)/admin/prizes/page.tsx
  - src/app/(admin)/admin/predictions/page.tsx
  - src/app/(admin)/admin/pre-season/_components/admin-pre-season-table.tsx
  - tests/lib/slug.test.ts
  - tests/components/member-link.test.tsx
  - tests/components/team-badge.test.tsx
  - tests/components/prediction-card.test.tsx
  - src/lib/reports/_data/gather-gameweek-data.ts
  - tests/reports/gather-data.test.ts
autonomous: true
requirements: [UI-01, UI-04]

must_haves:
  truths:
    - "Database has teams.primary_color, teams.secondary_color, members.favourite_team_id, seasons.ended_at"
    - "Tailwind v4 @theme in globals.css exposes pl-purple, pl-green, and supporting semantic tokens"
    - "All 20 PL teams have primary_color seeded from Wikipedia kit colours"
    - "Any member display_name rendered on admin + member surfaces is a clickable link to /members/[slug]"
    - "Prediction fixture cards show a home-team primary-colour left accent"
    - "toSlug('John Smith') === 'john-smith' and round-trips with DB functional index"
    - "bonus_awards.points_awarded DB CHECK constraint restricts values to (0, 20, 60) — matches code-level type"
    - "Gameweek report weeklyPoints are doubled when doubleBubbleActive is true — personal PDF, group PDF, and XLSX all agree"
  artifacts:
    - path: "supabase/migrations/012_polish_continuity.sql"
      provides: "Migration 012 — additive schema changes + seed + notification types"
      contains: "ALTER TABLE public.teams"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme tokens for PL palette"
      contains: "--color-pl-purple"
    - path: "src/lib/members/slug.ts"
      provides: "toSlug pure helper + findMemberBySlug async DB lookup"
      exports: ["toSlug", "findMemberBySlug"]
    - path: "src/components/shared/member-link.tsx"
      provides: "MemberLink component wrapping display name in Link to /members/[slug]"
      exports: ["MemberLink"]
  key_links:
    - from: "src/components/shared/member-link.tsx"
      to: "/members/[slug]"
      via: "Next Link with href derived from toSlug(displayName)"
      pattern: "href=\\{`/members/\\$\\{toSlug"
    - from: "src/components/fixtures/fixture-card.tsx"
      to: "teams.primary_color"
      via: "inline style or class consuming home team's primary_color hex"
      pattern: "primary_color"
---

<objective>
Ship the visual foundation for Phase 11: migration 012 (additive schema for team colours, favourite team FK, seasons archive marker, slug functional index), Tailwind v4 theme extension in globals.css (PL purple + green tokens), a reusable `<MemberLink>` helper + `toSlug` pure library, home-team colour accent on prediction fixture cards, and a site-wide mechanical replacement of plain `{displayName}` spans with `<MemberLink />` across ~12 admin and member surfaces.

Purpose: Establish the shared visual + linking primitives every subsequent Phase 11 plan depends on. `/members/[slug]` doesn't need to render yet (Plan 02 builds it) — the links can be created first because Next returns a 404 until the page exists, which is safe during development.

Output: One migration SQL, extended globals.css, two new shared utilities (slug + MemberLink), ~12 edited surfaces wired to MemberLink, tests for slug round-trip + MemberLink render + prediction-card colour accent + team-badge sanity.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-polish-continuity/11-CONTEXT.md
@.planning/phases/11-polish-continuity/11-RESEARCH.md

# Existing patterns to follow (read before coding)
@supabase/migrations/011_reports_export.sql
@supabase/migrations/010_championship_teams.sql
@src/app/globals.css
@src/components/team-badge.tsx
@src/components/fixtures/fixture-card.tsx

<interfaces>
<!-- Key patterns for executor. Tailwind v4 (no tailwind.config.ts exists). -->
<!-- Migration 011 is the most recent; copy its admin_notifications CHECK list verbatim. -->

Pattern: Tailwind v4 @theme extension (globals.css)
```css
@import "tailwindcss";
@theme inline {
  --color-pl-purple: #37003c;
  --color-pl-green: #00ff85;
  /* Optional scale: --color-pl-purple-50, --color-pl-purple-900 */
}
```
Usage in JSX: className="bg-pl-purple text-pl-green"

Pattern: admin_notifications CHECK ritual (Pitfall 3 — see 11-RESEARCH.md lines 399-403)
- DROP CONSTRAINT admin_notifications_type_check
- Re-ADD with ALL 23 existing types from migration 011 + 2 new ('season_archived','season_launched')
- Read migration 011 FIRST to copy the exact existing list; do NOT guess

Pattern: Functional unique index (from migration 010 line 35)
```sql
CREATE UNIQUE INDEX IF NOT EXISTS championship_teams_season_name_ci_idx
  ON public.championship_teams (season, lower(btrim(name)));
```
Phase 11 mirror:
```sql
CREATE UNIQUE INDEX IF NOT EXISTS members_display_name_slug_idx
  ON public.members (lower(btrim(replace(display_name, ' ', '-'))));
```

Pattern: Slug function (src/lib/members/slug.ts)
```ts
export function toSlug(displayName: string): string {
  return displayName.trim().toLowerCase().replace(/\s+/g, '-')
}
```
MUST match the Postgres functional-index expression exactly so DB uniqueness aligns with app slug.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 012 + theme tokens + team colour seeds</name>
  <files>
    supabase/migrations/012_polish_continuity.sql,
    src/app/globals.css,
    tests/components/team-badge.test.tsx,
    tests/components/prediction-card.test.tsx
  </files>
  <behavior>
    - Migration 012 applies cleanly to a DB that has migration 011 (idempotent via IF NOT EXISTS).
    - teams table has primary_color + secondary_color TEXT columns.
    - All 20 PL teams have primary_color populated after migration runs (verify with COUNT(*) WHERE primary_color IS NOT NULL = 20).
    - members table has favourite_team_id UUID NULL REFERENCES teams(id) ON DELETE SET NULL.
    - seasons table has ended_at TIMESTAMPTZ NULL.
    - Unique functional index members_display_name_slug_idx exists on lower(btrim(replace(display_name,' ','-'))).
    - admin_notifications CHECK constraint lists all 23 prior types PLUS 'season_archived' and 'season_launched' (copy verbatim from migration 011 — do NOT omit any).
    - bonus_awards.points_awarded has a CHECK constraint restricting values to (0, 20, 60). Matches the TypeScript return type on calculateBonusPoints. Defensive belt-and-braces to prevent bad manual edits via Supabase directly.
    - globals.css exposes --color-pl-purple (#37003c) and --color-pl-green (#00ff85) via @theme inline block.
    - TeamBadge component test: renders <img> with team crest src (smoke).
    - PredictionCard test: when home team has primary_color='#EF0107', card has a left-border style consuming that hex (either via inline style or class referencing the hex).
  </behavior>
  <action>
    1. Read supabase/migrations/011_reports_export.sql FIRST to extract the EXACT current admin_notifications_type_check list. Copy all types verbatim into migration 012.
    2. Create supabase/migrations/012_polish_continuity.sql with 7 sections: (1) teams colours ALTER, (2) members.favourite_team_id ALTER, (3) seasons.ended_at ALTER, (4) members slug functional UNIQUE index, (5) team colour seed UPDATEs, (6) admin_notifications CHECK drop+re-add, (7) bonus_awards.points_awarded CHECK constraint audit-fix. Use IF NOT EXISTS / `DO $$ ... END $$` guards everywhere. For section 7: `ALTER TABLE public.bonus_awards DROP CONSTRAINT IF EXISTS bonus_awards_points_awarded_check; ALTER TABLE public.bonus_awards ADD CONSTRAINT bonus_awards_points_awarded_check CHECK (points_awarded IN (0, 20, 60));` — this matches the TypeScript return type `0 | 20 | 60` on calculateBonusPoints and prevents invalid manual edits.
    3. For team colour seed: write one UPDATE per PL team using `WHERE lower(name) LIKE 'arsenal%'` style matches. Wikipedia infobox colours for all 20 current-season PL teams (Arsenal #EF0107/#FFFFFF, Aston Villa #670E36/#9FC5E8, Bournemouth #DA291C/#000000, Brentford #E30613/#FBB800, Brighton #0057B8/#FFFFFF, Chelsea #034694/#DBA111, Crystal Palace #1B458F/#C4122E, Everton #003399/#FFFFFF, Fulham #FFFFFF/#000000, Ipswich #4A90E2/#FFFFFF, Leicester #003090/#FDBE11, Liverpool #C8102E/#F6EB61, Man City #6CABDD/#1C2C5B, Man United #DA291C/#FBE122, Newcastle #241F20/#FFFFFF, Nottingham Forest #DD0000/#FFFFFF, Southampton #D71920/#FFFFFF, Spurs #132257/#FFFFFF, West Ham #7A263A/#1BB1E7, Wolves #FDB913/#231F20). Use IS NULL guard so re-runs don't clobber admin-edited values.
    4. Edit src/app/globals.css: add @theme inline block with --color-pl-purple and --color-pl-green (plus complementary shades if Tailwind v4 conventions apply — consult existing globals.css). Do NOT create tailwind.config.ts.
    5. Write failing tests first:
       - tests/components/team-badge.test.tsx: mounts <TeamBadge teamName="Arsenal" /> and asserts <img> renders.
       - tests/components/prediction-card.test.tsx: mounts <FixtureCard fixture={{ home_team: { primary_color: '#EF0107', ... }, away_team: {...}, ... }} /> and asserts the rendered element has a left-border style or class containing '#EF0107'. Use @testing-library queries (getByRole/getByText).
    6. If fixture-card.tsx doesn't already read primary_color, wire it: add `style={{ borderLeft: `4px solid ${fixture.home_team.primary_color ?? 'transparent'}` }}` to the card's outer wrapper div. Graceful null fallback.
    7. Run tests — they should go green once fixture-card wiring is done.

    AVOID:
    - Creating or editing tailwind.config.ts (Pitfall 1 — file doesn't exist; Tailwind v4 uses @theme in CSS).
    - Truncating admin_notifications CHECK list (Pitfall 3 — must include all existing types).
    - Physically moving data between seasons (Pattern 1 — column versioning only).
  </action>
  <verify>
    <automated>npm run test:run -- tests/components/team-badge.test.tsx tests/components/prediction-card.test.tsx</automated>
  </verify>
  <done>
    - Migration 012 file committed with all 6 sections.
    - globals.css exposes pl-purple + pl-green tokens.
    - 20 UPDATE statements for team colours present.
    - team-badge test passes. prediction-card test passes with home-team primary-colour left border visible.
    - admin_notifications CHECK DROPs + re-ADDs with 23 prior types + 2 new ones.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: toSlug helper + MemberLink component + tests</name>
  <files>
    src/lib/members/slug.ts,
    src/components/shared/member-link.tsx,
    tests/lib/slug.test.ts,
    tests/components/member-link.test.tsx
  </files>
  <behavior>
    - toSlug('John Smith') === 'john-smith'
    - toSlug('  John  Smith  ') === 'john-smith' (whitespace collapse + trim)
    - toSlug('John   Smith') === 'john-smith' (multi-space collapse)
    - toSlug('JOHN SMITH') === 'john-smith' (lowercase)
    - toSlug('Dave') === 'dave' (single word, no dash)
    - Slug output matches Postgres functional-index expression: lower(btrim(replace(x,' ','-'))) — meaning the function does trim, lowercase, space-to-dash. Verify multi-space edge case is deterministic (replace(/\s+/, '-') handles it; document the minor divergence from raw replace(' ','-') but assert it yields a slug the DB index still accepts uniquely).
    - <MemberLink displayName="John Smith" /> renders an <a> element with href="/members/john-smith" and the display name as visible text.
    - MemberLink accepts optional className prop and merges it with defaults.
    - MemberLink has a hover state via className token (e.g. "hover:text-pl-green").
  </behavior>
  <action>
    1. Write failing tests first:
       - tests/lib/slug.test.ts: cases above for toSlug.
       - tests/components/member-link.test.tsx: render with displayName="John Smith", assert getByRole('link').href ends with '/members/john-smith' and link text is 'John Smith'. Second test: className prop merges.
    2. Create src/lib/members/slug.ts:
       ```ts
       export function toSlug(displayName: string): string {
         return displayName.trim().toLowerCase().replace(/\s+/g, '-')
       }
       ```
       Do NOT pull any slugify dependency.
    3. Also export findMemberBySlug async helper in slug.ts — takes a supabase admin client + slug string, queries `select * from members` with a server-side match on lower(btrim(replace(display_name,' ','-'))) = slug (use .filter() or .rpc() if cleaner). Return MemberRow | null. Note: this is for Plan 02 consumption, but export the function signature here so Plan 02 has the contract.
    4. Create src/components/shared/member-link.tsx matching Example 2 in RESEARCH (Link from next/link, href=`/members/${toSlug(displayName)}`, default className with hover:text-pl-green transition).
    5. Run tests — expect green.

    AVOID:
    - Adding a `slugify` npm dep.
    - Generating slug in database via trigger (app-side helper suffices — uniqueness is enforced by the functional index from Task 1).
  </action>
  <verify>
    <automated>npm run test:run -- tests/lib/slug.test.ts tests/components/member-link.test.tsx</automated>
  </verify>
  <done>
    - slug.ts exports toSlug + findMemberBySlug.
    - member-link.tsx exports MemberLink with hover accent.
    - All slug edge-case tests green.
    - MemberLink renders correct href and text.
  </done>
</task>

<task type="auto">
  <name>Task 3: Site-wide MemberLink replacement (bundled by surface)</name>
  <files>
    src/components/predictions/predictions-table.tsx,
    src/components/los/admin-los-table.tsx,
    src/components/los/los-standings.tsx,
    src/components/admin/confirm-bonus-awards.tsx,
    src/components/admin/confirm-prize-dialog.tsx,
    src/components/admin/member-table.tsx,
    src/app/(public)/standings/page.tsx,
    src/app/(member)/gameweeks/[gwNumber]/page.tsx,
    src/app/(member)/bonuses/page.tsx,
    src/app/(admin)/admin/bonuses/page.tsx,
    src/app/(admin)/admin/prizes/page.tsx,
    src/app/(admin)/admin/predictions/page.tsx,
    src/app/(admin)/admin/pre-season/_components/admin-pre-season-table.tsx
  </files>
  <action>
    Bundle by surface area, not per file. Three sub-batches, each committed as one coherent change:

    **Admin surfaces** — confirm-bonus-awards.tsx (line 123), confirm-prize-dialog.tsx (line 27 — read-only display, link optional; wrap it for consistency), member-table.tsx (line 158), admin/bonuses/page.tsx, admin/prizes/page.tsx, admin/predictions/page.tsx, admin/pre-season/_components/admin-pre-season-table.tsx, predictions/predictions-table.tsx (line 107), los/admin-los-table.tsx (line 192). Replace every `<span>{m.display_name}</span>` (or equivalent naked text rendering of a member's display name) with `<MemberLink displayName={m.display_name} />`. Import the component at the top of each file.

    **Member surfaces** — los/los-standings.tsx (line 87), (member)/gameweeks/[gwNumber]/page.tsx, (member)/bonuses/page.tsx.

    **Public surface** — (public)/standings/page.tsx. IMPORTANT: this is the unauth landing page; the link target `/members/[slug]` lives under (member) route group so unauth clicks will redirect to /signin. Per 11-RESEARCH.md Pitfall 5 + locked CONTEXT decision, this behaviour is intentional and acceptable.

    **Out of scope (do NOT modify):**
    - signup-form.tsx (form input, not display)
    - member-actions.tsx (handler)
    - dashboard-overview.tsx line 27 (own name; no self-link)
    - import-preview-table.tsx (not a live member — imported preview row)
    - email templates src/emails/* (PDF context — not clickable)
    - Any file with `display_name` used only in a Zod schema, type def, or sort comparator

    For each edited file:
    1. Add `import { MemberLink } from '@/components/shared/member-link'` at the top.
    2. Find the exact line where the member's display_name is rendered as naked text (e.g. `{m.display_name}` inside a `<td>` or `<span>` with no link wrapper).
    3. Replace with `<MemberLink displayName={m.display_name} />`.
    4. Preserve surrounding styling — pass className through if the original span had styling that should persist.
    5. Do not change sort keys, table headers, column widths, or any non-display usage.

    After all edits, run `npm run build` to catch any import path / TS errors site-wide.

    AVOID:
    - One commit per file (bundle by admin/member/public surface).
    - Changing email template display_name renderings (Pitfall 7 — emails are separate).
    - Converting form labels or placeholders that happen to show a name (those aren't "displayed" in the link sense).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - All 12 target files import MemberLink.
    - Every admin + member surface with a displayed member name links to /members/[slug].
    - `npm run build` green.
    - Full test suite green: `npm run test:run`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Double Bubble display-multiplier fix in gameweek report aggregator (pre-launch audit fix, HIGH)</name>
  <why>
    Pre-launch scoring audit (2026-04-12) identified that `src/lib/reports/_data/gather-gameweek-data.ts` aggregates `weeklyPoints` per member WITHOUT applying the Double Bubble ×2 multiplier when `doubleBubbleActive` is true. The flag is passed through on the returned shape but never used inside the aggregator. Consequences: Personal PDF and Group PDF show RAW weekly totals on GW10/20/30; the admin XLSX correctly doubles them (different code path). Members would receive an email showing 45pts while George's XLSX says 90pts for the same GW — confusion + disputes guaranteed. Raw DB data is correct; this is a display-layer-only bug in one file. Priority: HIGH — must ship before any real Double Bubble week email fires.
  </why>
  <files>
    src/lib/reports/_data/gather-gameweek-data.ts,
    tests/reports/gather-data.test.ts
  </files>
  <behavior>
    - gatherGameweekData returns a `doubleBubbleActive` boolean (already does — keep as-is).
    - Each member's `weeklyPoints` in the returned shape reflects the DISPLAY value: raw base + confirmed bonus, then ×2 if doubleBubbleActive is true. Pending bonuses still excluded from the figure (already correct).
    - RUNNING season total (`seasonTotal` or equivalent field) ALSO reflects the doubled value contribution for any Double Bubble week that has already closed — verify this is consistent across the aggregator. If the season total is computed by summing confirmed weekly displayed totals from prior GWs, it needs the same fix retrospectively; otherwise it should already be correct via the prediction_scores + bonus_awards reads for prior weeks.
    - The group PDF and personal PDF renderers consuming gatherGameweekData now show correct doubled totals without ANY changes needed in their own code.
    - The admin XLSX (which already applied the multiplier in its own code) continues to produce the same value — no regression.
  </behavior>
  <action>
    TDD (RED → GREEN):

    1. **Write failing tests FIRST** — extend `tests/reports/gather-data.test.ts`:
       - Test: "applies Double Bubble multiplier to weeklyPoints when gameweek.double_bubble is true"
         Given: a gameweek with `double_bubble: true`, one member with 40 base + 20 confirmed bonus (raw 60). Expected `weeklyPoints = 120`.
       - Test: "does NOT double pending bonuses even when Double Bubble active"
         Given: one member with 40 base + 20 pending bonus + 0 confirmed. Pending excluded, so raw 40. DoubleBubble ×2 = 80. Expected `weeklyPoints = 80` (not 120).
       - Test: "does NOT apply multiplier when gameweek.double_bubble is false"
         Given: same as first test but `double_bubble: false`. Expected `weeklyPoints = 60`.
       - Test: "keeps doubleBubbleActive flag on returned shape regardless of multiplier application"
         Given: `double_bubble: true` gameweek. Expected `doubleBubbleActive: true` in returned shape.
       - Run: `npm run test:run -- tests/reports/gather-data.test.ts` — expect 4 new failures.

    2. **Implement GREEN** — `src/lib/reports/_data/gather-gameweek-data.ts`:
       - After the weeklyByMember Map is fully populated (raw base + confirmed bonus per member), wrap the final assignment with a multiplier: `if (gameweek.double_bubble) { for (const [id, pts] of weeklyByMember) weeklyByMember.set(id, pts * 2); }`.
       - Place this transform BEFORE the shape is returned — so all downstream consumers (group PDF, personal PDF) receive the displayed value.
       - Do NOT touch the admin XLSX code path — it applies its own multiplier correctly and reading the already-doubled value would double-count. Confirm the XLSX code currently reads raw fields, not the aggregator's weeklyPoints, before shipping. If the XLSX ALSO consumes gatherGameweekData.weeklyPoints (same function), REMOVE the redundant multiplication in the XLSX renderer to avoid double-doubling (`4×` bug). Verify via grep: `grep -n "weeklyPoints" src/lib/reports/weekly-xlsx.ts src/lib/reports/full-export-xlsx.ts`.

    3. **Review seasonTotal logic** — if gatherGameweekData also computes a running season total, ensure that number aggregates DISPLAYED weekly totals for each prior GW (already-doubled where applicable). If it reads prediction_scores + bonus_awards directly per-GW, it naturally needs the same treatment for each historical DB week flagged `double_bubble: true`. If that's more involved than this plan's scope, write the test anyway (expected behaviour) and comment the function with a TODO referencing the follow-up — but attempt the fix first.

    4. **Run full suite** — `npm run test:run` — expect 536/536 baseline + 4 new Plan-01-Task-4 tests green (540/540 total, plus any other Plan 01 additions).

    5. **Spot-check XLSX behavior manually** — simulate a GW10 closure in a dev environment if possible; verify group PDF shows doubled totals AND admin XLSX shows the same numbers (no 4× bug from double-doubling).
  </action>
  <verify>
    <automated>npm run test:run -- tests/reports/gather-data.test.ts tests/reports/weekly-xlsx.test.ts tests/reports/full-export.test.ts</automated>
  </verify>
  <done>
    - 4 new failing tests added RED, then green after implementation
    - gather-gameweek-data.ts applies Double Bubble ×2 multiplier to weeklyPoints before returning
    - Existing XLSX tests still pass (no double-doubling regression)
    - Full test suite green
    - Member PDFs and admin XLSX will show identical weekly totals on GW10/20/30
  </done>
</task>

</tasks>

<verification>
- Migration 012 applies cleanly against a Phase-10-complete DB.
- `npm run test:run` green (existing 536 + Plan 01 additions).
- `npm run build` green.
- Visual smoke (deferred to Phase 11 master QA §13): load /standings and click a name — should route to /members/[slug] which will 404 until Plan 02 lands (acceptable mid-phase).
- Tailwind class `bg-pl-purple` produces #37003c background in compiled CSS.
</verification>

<success_criteria>
- Migration 012 committed with all schema + seed + CHECK-constraint work (including bonus_awards audit-fix CHECK).
- Tailwind v4 theme tokens for PL palette visible in globals.css.
- toSlug + MemberLink shipped with green tests.
- Site-wide clickable-usernames rewrite complete across all in-scope admin/member/public surfaces.
- Prediction fixture cards show home-team primary-colour left accent.
- Double Bubble ×2 multiplier applied inside gather-gameweek-data.ts — PDFs + XLSX agree on weekly totals for GW10/20/30.
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-continuity/11-01-SUMMARY.md` capturing: which MemberLink target files were edited, total new test count, any deferred items (e.g. email template colour refresh if split out), and the exact team-colour hex list seeded.
</output>
