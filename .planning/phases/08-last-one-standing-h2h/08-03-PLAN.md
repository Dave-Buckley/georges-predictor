---
phase: 08-last-one-standing-h2h
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/actions/admin/los.ts
  - src/actions/admin/gameweeks.ts
  - src/app/(admin)/admin/los/page.tsx
  - src/app/(member)/los/page.tsx
  - src/app/(member)/gameweek/[number]/page.tsx
  - src/components/admin/sidebar.tsx
  - src/components/los/admin-los-table.tsx
  - src/components/los/los-status-card.tsx
  - src/components/los/los-standings.tsx
  - src/components/h2h/h2h-steal-banner.tsx
  - tests/actions/admin/los.test.ts
autonomous: false
requirements: [LOS-01, LOS-04, LOS-07, H2H-01, H2H-02]

must_haves:
  truths:
    - "Admin LOS page at /admin/los shows a table of every member's status, current pick, teams used (count + expandable list), eliminated_at_gw"
    - "Admin can override-eliminate a member via dialog (reason='admin_override')"
    - "Admin can reinstate an eliminated member (sets status='active', clears eliminated_at_gw)"
    - "Admin can manually trigger competition reset (fallback for edge cases)"
    - "Admin actions are gated by requireAdmin() — non-admins get 403 on server action call"
    - "Admin sidebar includes 'Last One Standing' link using Crown icon"
    - "closeGameweek admin action triggers detectH2HForGameweek for the closed gameweek + resolveStealsForGameweek for steals landing in it"
    - "Member LOS page at /los shows their own status (in/eliminated), current pick, teams used, teams remaining count, list of members still in"
    - "Member gameweek page shows H2H steal banner when a steal is active involving them or landing in the current gameweek"
    - "Member cannot see other members' current-GW LOS picks (RLS enforced — verified by spot-check)"
  artifacts:
    - path: "src/actions/admin/los.ts"
      provides: "requireAdmin-gated server actions: overrideEliminate, reinstateMember, resetCompetitionManually, setLosPickForMember (admin correction)"
      exports: ["overrideEliminate", "reinstateMember", "resetCompetitionManually", "setLosPickForMember"]
      min_lines: 100
    - path: "src/actions/admin/gameweeks.ts"
      provides: "Extended closeGameweek to invoke detectH2HForGameweek + resolveStealsForGameweek"
      contains: "detectH2HForGameweek"
    - path: "src/app/(admin)/admin/los/page.tsx"
      provides: "Admin LOS page — server component fetching competition + members + picks; renders AdminLosTable"
      min_lines: 50
    - path: "src/app/(member)/los/page.tsx"
      provides: "Member LOS status page"
      min_lines: 40
    - path: "src/components/los/admin-los-table.tsx"
      provides: "Client component: member rows, status badges, action buttons (override/reinstate/reset), team usage chips"
      exports: ["AdminLosTable"]
      min_lines: 100
    - path: "src/components/los/los-status-card.tsx"
      provides: "Member's own LOS status (in/eliminated banner, current pick, teams remaining count)"
      exports: ["LosStatusCard"]
    - path: "src/components/los/los-standings.tsx"
      provides: "List of active members in current competition, ordered: active first then by teams-used-count ASC, alphabetical tiebreak"
      exports: ["LosStandings"]
    - path: "src/components/h2h/h2h-steal-banner.tsx"
      provides: "Banner shown on gameweek page when an active h2h_steal involves the viewing member or resolves in that gameweek"
      exports: ["H2HStealBanner"]
    - path: "src/components/admin/sidebar.tsx"
      provides: "Extended navItems with LOS link (Crown icon)"
      contains: "/admin/los"
    - path: "tests/actions/admin/los.test.ts"
      provides: "Integration tests for admin actions — requireAdmin gating, override/reinstate flows, manual reset"
      min_lines: 120
  key_links:
    - from: "src/actions/admin/los.ts"
      to: "src/lib/los/round.ts"
      via: "resetCompetitionManually calls resetCompetitionIfNeeded with a manual winner_id or null"
      pattern: "resetCompetitionIfNeeded"
    - from: "src/actions/admin/gameweeks.ts"
      to: "src/lib/h2h/sync-hook.ts"
      via: "closeGameweek calls detectH2HForGameweek + resolveStealsForGameweek"
      pattern: "detectH2HForGameweek"
    - from: "src/components/admin/sidebar.tsx"
      to: "src/app/(admin)/admin/los/page.tsx"
      via: "navItems href entry"
      pattern: "'/admin/los'"
    - from: "src/app/(member)/gameweek/[number]/page.tsx"
      to: "src/components/h2h/h2h-steal-banner.tsx"
      via: "Page renders H2HStealBanner when relevant steal exists"
      pattern: "H2HStealBanner"
---

<objective>
Admin and member UI surfaces for LOS + H2H, plus wiring `detectH2HForGameweek` into the gameweek-close flow.

Purpose: This is the visible layer — George gets a dashboard to manage LOS, members see their status, H2H steals surface on the gameweek page. Plan 02 built the engine; Plan 03 exposes it. After this plan, Phase 8 is user-facing complete.

Output: Admin LOS management page with override/reinstate/reset actions, member LOS status page, H2H steal banner on the gameweek page, sidebar link, integrated H2H detection on gameweek close, final manual-QA checkpoint.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-last-one-standing-h2h/08-CONTEXT.md
@.planning/phases/08-last-one-standing-h2h/08-RESEARCH.md
@.planning/phases/08-last-one-standing-h2h/08-01-SUMMARY.md
@.planning/phases/08-last-one-standing-h2h/08-02-SUMMARY.md
@.planning/phases/05-admin-panel/05-02-SUMMARY.md
@.planning/phases/05-admin-panel/05-03-SUMMARY.md
@src/actions/admin/bonuses.ts
@src/actions/admin/gameweeks.ts
@src/components/admin/sidebar.tsx
@src/components/admin/set-bonus-dialog.tsx
@src/lib/los/round.ts
@src/lib/h2h/sync-hook.ts
@src/lib/validators/los.ts

<interfaces>
<!-- From Plan 02 (already implemented). Import these directly. -->

From src/lib/los/round.ts:
```typescript
export async function resetCompetitionIfNeeded(
  adminClient, competitionId, endedAtGw, winnerId, season
): Promise<{ newCompetitionId: string }>
```

From src/lib/h2h/sync-hook.ts:
```typescript
export async function detectH2HForGameweek(adminClient, gameweekId): Promise<{ stealsCreated: number }>
export async function resolveStealsForGameweek(adminClient, resolvesInGwId): Promise<{ resolvedCount: number }>
```

From src/lib/validators/los.ts:
```typescript
export const adminOverrideEliminateSchema = z.object({
  member_id: z.string().uuid(),
  competition_id: z.string().uuid(),
  reason: z.enum(['draw','lose','missed','admin_override']),
})
export const adminReinstateSchema = z.object({
  member_id: z.string().uuid(),
  competition_id: z.string().uuid(),
})
```

Existing admin auth pattern (src/actions/admin/bonuses.ts):
```typescript
'use server'
import { requireAdmin } from '@/actions/admin/auth'
import { createAdminClient } from '@/lib/supabase/admin'

export async function overrideEliminate(formData: FormData) {
  await requireAdmin()
  const parsed = adminOverrideEliminateSchema.safeParse(Object.fromEntries(formData))
  if (!parsed.success) return { error: parsed.error.issues[0]?.message ?? 'Invalid input' }
  const adminClient = createAdminClient()
  // ... update los_competition_members ...
  revalidatePath('/admin/los')
  return { success: true }
}
```

Sidebar pattern (src/components/admin/sidebar.tsx):
```typescript
// navItems array — add Phase 8 entries after existing items.
import { Crown } from 'lucide-react'
{ href: '/admin/los', label: 'Last One Standing', icon: Crown },
```

Admin dialog pattern (see src/components/admin/set-bonus-dialog.tsx, close-gameweek-dialog.tsx):
- Radix Dialog + form with server action submission
- Error + success handling via useState + toast

LOS standings ordering rule (RESEARCH Open Question 5):
- Active members first, eliminated after
- Within active: teams-used-count ASC (fewer used = more options remaining)
- Within eliminated: eliminated_at_gw DESC
- Alphabetical (display_name) as tiebreak

H2H steal banner contract:
- Fetch active h2h_steals WHERE resolves_in_gw_id = viewingGwId AND resolved_at IS NULL
- Also fetch resolved steals for this GW to show "steal resolved — winner: X"
- Highlight tied members; if viewing member is tied, show "You're in an H2H steal — compete next week to win the jackpot"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin server actions (override/reinstate/reset/setPick) + closeGameweek extension</name>
  <files>src/actions/admin/los.ts, src/actions/admin/gameweeks.ts, tests/actions/admin/los.test.ts</files>
  <behavior>
LOS-04, LOS-07 (admin actions):
- overrideEliminate: valid input from admin → los_competition_members row updated (status='eliminated', reason='admin_override', eliminated_at_gw=currentGw.number). Returns success.
- overrideEliminate: called by non-admin → requireAdmin throws; action returns error. No DB mutation.
- overrideEliminate: already-eliminated member → no-op (or error 'Already eliminated'); either is acceptable but consistent.
- reinstateMember: valid input from admin → los_competition_members row updated (status='active', eliminated_at_gw=null, eliminated_reason=null).
- reinstateMember: called with non-eliminated member → error 'Member is not eliminated'.
- resetCompetitionManually: admin supplies (competition_id, winner_id | null) → if winner_id, calls resetCompetitionIfNeeded with it; if null, marks current complete with NO winner_id and starts new competition anyway (explicit George override).
- setLosPickForMember: admin can set or clear a member's pick for a specific gameweek (corrects mistakes). Bypasses kickoff guard. Enforces team-not-already-used in cycle (reuses validation from submitPredictions).

H2H wiring:
- closeGameweek (existing Phase 5 action) extended: after marking closed_at, calls detectH2HForGameweek(adminClient, gameweekId).
- Also calls resolveStealsForGameweek(adminClient, gameweekId) to resolve any steals landing in this GW.
- If either fails, gameweek-close still succeeds (errors logged to admin_notifications type='system') — H2H detection is non-blocking.
  </behavior>
  <action>
**Step 1 — TDD**: Write `tests/actions/admin/los.test.ts` with Vitest. Scenarios above. Mock `requireAdmin` + adminClient. Run RED.

**Step 2 — `src/actions/admin/los.ts`**:

`'use server'` header. Import requireAdmin + createAdminClient + revalidatePath + validators.

Implement four actions following `src/actions/admin/bonuses.ts` idiom exactly:

1. **overrideEliminate(formData)** — parse with `adminOverrideEliminateSchema`; load current gameweek to derive `eliminated_at_gw`; UPDATE los_competition_members WHERE (competition_id, member_id); insert admin_notification type='system' with metadata `{action: 'los_override_eliminate', member_id, competition_id}`; revalidatePath('/admin/los').

2. **reinstateMember(formData)** — parse with `adminReinstateSchema`; verify row currently has status='eliminated' (if not, return error); UPDATE to status='active', eliminated_at_gw=null, eliminated_reason=null; revalidate.

3. **resetCompetitionManually(formData)** — parse `{ competition_id, winner_id?: string, season: number, ended_at_gw: number }`. Call `resetCompetitionIfNeeded` (from Plan 02) with supplied winner_id or a sentinel. If no winner_id, UPDATE current competition (status='complete', ended_at_gw, ended_at=now()) then insert new competition (reuse the function's new-competition-insertion logic). Consider refactoring Plan 02's `resetCompetitionIfNeeded` to accept `winnerId: string | null` if needed — document deviation in SUMMARY.

4. **setLosPickForMember(formData)** — parse `{ member_id, competition_id, gameweek_id, team_id | null }`. If team_id is null → DELETE los_picks row. Else → resolve fixture (reuse logic from submitPredictions — extract to shared helper `src/lib/los/resolve-fixture.ts` if cleaner), validate team not already used in cycle, UPSERT los_picks.

**Step 3 — Extend `src/actions/admin/gameweeks.ts`**:

Locate existing `closeGameweek` action. After the DB update that sets `closed_at`, add:

```typescript
// Non-blocking H2H integration
try {
  await detectH2HForGameweek(adminClient, gameweekId)
  await resolveStealsForGameweek(adminClient, gameweekId)
} catch (error) {
  // Log but don't fail the close operation
  await adminClient.from('admin_notifications').insert({
    type: 'system',
    metadata: { action: 'h2h_detect_failed', gameweek_id: gameweekId, error: String(error) },
  })
}
```

**Step 4 — Run tests**: `npm run test -- tests/actions/admin/los.test.ts tests/actions/admin/gameweeks.test.ts --run`. All green.

Commit: `test(08-03): add failing tests for admin LOS actions` → `feat(08-03): implement admin LOS actions + H2H close hook`.
  </action>
  <verify>
    <automated>npm run test -- tests/actions/admin/los.test.ts --run</automated>
  </verify>
  <done>Four admin actions exported with requireAdmin gating; closeGameweek triggers H2H detection + resolution (non-blocking); all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: UI — Admin LOS page, member LOS page, H2H banner, sidebar wiring</name>
  <files>src/app/(admin)/admin/los/page.tsx, src/app/(member)/los/page.tsx, src/components/los/admin-los-table.tsx, src/components/los/los-status-card.tsx, src/components/los/los-standings.tsx, src/components/h2h/h2h-steal-banner.tsx, src/components/admin/sidebar.tsx, src/app/(member)/gameweek/[number]/page.tsx</files>
  <action>
**Step 1 — Admin sidebar** (`src/components/admin/sidebar.tsx`):
Import `Crown` from `lucide-react`. Add to navItems array (after existing Phase 5/6/7 entries like 'Prizes' and 'Import'):
```typescript
{ href: '/admin/los', label: 'Last One Standing', icon: Crown },
```
No separate H2H link — H2H shown inline on the admin dashboard + gameweek page.

**Step 2 — Admin LOS page** (`src/app/(admin)/admin/los/page.tsx`):

Server component. Use `requireAdmin()` at top. Fetch with adminClient:
- Active `los_competitions` row (competition_num, starts_at_gw, created_at).
- All `los_competition_members` for active competition, JOINed with members (display_name, email).
- All `los_picks` for active competition, JOINed with gameweeks.number, teams (name, badge_url).
- Current gameweek (for displaying "current pick" only from that GW).

Group picks by member_id (history) and identify each member's current pick (the pick for the current gameweek, if any).

Render `<AdminLosTable />` (client component) with this data + pass the 4 server actions as props (or reference them via `formAction`).

If no active competition exists: show "No active LOS competition" + button calling `resetCompetitionManually` to start one.

**Step 3 — `src/components/los/admin-los-table.tsx`** (client):

Columns: Member | Status (badge: green 'In' / gray 'Out') | Current Pick (team badge + name, or '—' if none) | Teams Used (count + expandable chip row showing badges) | Eliminated GW (number or '—') | Actions.

Actions column:
- If active: "Override Eliminate" button → opens Radix Dialog with reason select + confirm (calls overrideEliminate).
- If eliminated: "Reinstate" button → confirm dialog (calls reinstateMember).
- Admin pick correction: "Set Pick" icon button → dialog to pick a team for current GW (calls setLosPickForMember).

Ordering: active first (teams-used-count ASC, alpha tiebreak), then eliminated (eliminated_at_gw DESC, alpha).

Use established dialog style (`src/components/admin/close-gameweek-dialog.tsx` as reference).

Header bar: "Competition #N · Started GW {starts_at_gw}" + "Reset competition manually" button (confirms first).

**Step 4 — Member LOS page** (`src/app/(member)/los/page.tsx`):

Server component. Use session client (RLS enforced). Fetch:
- Active competition.
- Current user's `los_competition_members` row (status).
- Current user's pick history (los_picks WHERE competition_id, ORDER BY gameweek_id).
- All active members' names in the competition (allowed — public roster info; do NOT expose their current pick until kickoff per RLS).

Render:
- `<LosStatusCard />`: In/Out banner, current pick (from current GW), teams remaining count = 20 - history.length (accounting for cycle reset).
- `<LosStandings />`: list of active members (name + teams-used count, badge grid optional).
- If eliminated: message "You've been eliminated. Next competition starts when a winner is found."
- If no active competition: message "No active Last One Standing competition right now."

**Step 5 — `src/components/los/los-status-card.tsx`** (client or server — can be server):

Props: `{ status: 'active'|'eliminated', currentPick?: {team, gameweekNumber}, teamsUsedCount: number, teamsRemaining: number }`. Render card with TeamBadge for current pick, status pill, counts.

**Step 6 — `src/components/los/los-standings.tsx`**:

Props: `{ activeMembers: Array<{id, display_name, teamsUsedCount}> }`. Render sorted list per ordering rule (teams-used ASC, alpha tiebreak).

**Step 7 — `src/components/h2h/h2h-steal-banner.tsx`**:

Props: `{ steal: H2hStealRow, tiedMemberNames: string[], viewerIsTied: boolean, position: 1 | 2, stage: 'detected' | 'resolving' | 'resolved', winnerNames?: string[] }`.

Display variants:
- **detected (flagged for next week)**: "H2H Steal flagged for {position===1 ? 'jackpot (£30)' : 'runner-up (£10)'} — tied members: {names}. Next gameweek determines the winner."
- **resolving (this is the resolution GW)**: "H2H Steal resolving this gameweek — highest scorer between {names} wins."
- **resolved**: "H2H Steal resolved — winner(s): {winnerNames}" (or "jackpot split" if multiple).

Use amber color accent for 'detected' + 'resolving'; green for 'resolved'.

**Step 8 — Extend `src/app/(member)/gameweek/[number]/page.tsx`**:

Add data fetch for h2h_steals:
- Steals WHERE detected_in_gw_id = viewingGw.id (shows as 'detected' — flagged for NEXT week).
- Steals WHERE resolves_in_gw_id = viewingGw.id (shows as 'resolving' or 'resolved' depending on resolved_at).

Resolve member names for tied_member_ids + winner_ids. Render `<H2HStealBanner />` near top of page for each relevant steal.

**Step 9 — Build & lint**:
- `npm run build` must succeed (Next.js 16 App Router).
- `npm run lint` must pass.
- Full test suite `npm run test:run` green.

Commit: `feat(08-03): admin LOS page + member LOS view + H2H banner`.
  </action>
  <verify>
    <automated>npm run build && npm run test:run</automated>
  </verify>
  <done>Admin LOS page accessible + renders table with override/reinstate/reset actions; member /los page renders status + standings; H2H banner appears on gameweek page when steals exist; sidebar link present; build green; all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual QA — mobile LOS picker + admin table + H2H banner visual check</name>
  <files>(no files — human verification only)</files>
  <action>
PAUSE FOR HUMAN VERIFICATION. Phase 8 automated work is complete. User must manually verify the following on a running dev server before the phase is signed off.

**What was built:** LOS competition state machine end-to-end, H2H steal detection and resolution, admin management UI, member status surfaces. All automated tests green and build succeeds.

**How to verify (run `npm run dev` locally first):**

1. **Admin LOS page** (visit `/admin/los` as George):
   - Sidebar: "Last One Standing" link visible with crown icon
   - If no active competition: "Start new competition" button works
   - Table renders with correct columns (Member, Status, Current Pick, Teams Used, Eliminated GW, Actions)
   - "Override Eliminate" dialog opens, reason dropdown works, confirm mutates DB (check row shows eliminated)
   - "Reinstate" dialog works on eliminated members
   - "Reset competition manually" confirms + creates a fresh competition row in DB
   - Ordering: active first sorted by teams-used ASC alphabetical tiebreak; eliminated after

2. **Member LOS pick picker on mobile** (visit `/gameweek/[current]` as a non-eliminated member on a phone screen — use DevTools responsive mode set to iPhone 13 + Android Pixel 5):
   - LosTeamPicker renders above submit button
   - Tapping opens Radix Select sheet (full width on mobile)
   - Team list shows only teams NOT already used in current competition
   - Team badges display alongside names
   - Selecting persists value; form can be submitted
   - Submitting WITHOUT a pick (while active) shows blocking error
   - Submitting WITH valid pick saves (check los_picks in DB)

3. **Member LOS status page** (visit `/los`):
   - Status card shows current pick + teams remaining count
   - Standings list sorted correctly
   - If eliminated: read-only banner shows

4. **H2H steal banner** (manually insert a test h2h_steals row via Supabase SQL editor):
   - Visit gameweek page where `detected_in_gw_id` matches → see "flagged for next week" banner
   - Visit gameweek page where `resolves_in_gw_id` matches + `resolved_at IS NULL` → see "resolving this week" banner
   - After close + resolve, visit that gameweek → see "resolved — winner: X" banner
   - Verify copy clarity (non-technical audience per user-george-technical memory)

5. **Notifications** (check admin dashboard):
   - Trigger a LOS winner scenario (manually set survivors to 1 in test data + run `runLosRound` via a test harness, or close a GW with a known tie) → 'los_winner_found' + 'los_competition_started' notifications appear
   - Trigger H2H close → 'h2h_steal_detected' notification appears

6. **RLS spot-check** (log in as member, inspect Network tab):
   - Query los_picks for a gameweek whose fixtures haven't all kicked off — should only return own row
   - Query los_picks for a gameweek whose fixtures have all kicked off — can return all members' picks
  </action>
  <verify>
Human verification only — no automated check. Resume by typing "approved" if all 6 checks pass, or describe any issues to generate gap-closure tasks.
  </verify>
  <done>User has manually confirmed all 6 verification scenarios; Phase 8 signed off.</done>
  <resume-signal>Type "approved" to finalize Phase 8, or describe issues and I'll create gap-closure tasks.</resume-signal>
</task>

</tasks>

<verification>
Plan 03 verification:
1. `npm run test:run` — full suite green including new admin action tests.
2. `npm run build` — Next.js production build succeeds.
3. `npm run lint` — no lint errors on new files.
4. Admin sidebar renders LOS link.
5. Member on eliminated status sees read-only banner (not picker).
6. closeGameweek triggers H2H detection + resolution without blocking close operation on H2H failure.
7. Manual QA checkpoint signed off.
</verification>

<success_criteria>
- Every LOS/H2H requirement (LOS-01 through LOS-07, H2H-01 through H2H-03) is now testable end-to-end in the running app.
- George has a single page (`/admin/los`) to manage the entire sub-competition with override capabilities.
- Members have a dedicated status page (`/los`) and see LOS + H2H context inline with predictions.
- H2H tie detection runs automatically on gameweek close — zero manual work for George.
- All user decisions from 08-CONTEXT.md honored:
  * Dropdown picker (Radix Select) ✓
  * Mandatory when active + eligible ✓
  * Hidden when eliminated ✓
  * Admin override + reinstate ✓
  * Auto-reset on winner (with admin notification + manual reset fallback) ✓
  * H2H auto-detect + auto-resolve + admin notification ✓
  * Member sees H2H steal status on gameweek page ✓
</success_criteria>

<output>
After completion, create `.planning/phases/08-last-one-standing-h2h/08-03-SUMMARY.md`:
- Admin action signatures as implemented
- Final UI layout decisions (any deviations from plan)
- Edge cases discovered during manual QA
- Hand-off notes for Phase 10 (Reports): how to query LOS history + H2H steal log for weekly reports
- Confirmation that Phase 8 requirements are all marked complete in REQUIREMENTS.md
</output>
