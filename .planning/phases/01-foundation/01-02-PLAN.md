---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/actions/auth.ts
  - src/lib/validators/auth.ts
  - src/app/(public)/page.tsx
  - src/app/(public)/signup/page.tsx
  - src/components/auth/signup-form.tsx
  - src/components/auth/name-picker.tsx
  - src/app/(public)/layout.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-09

must_haves:
  truths:
    - "A visitor can pick their name from a dropdown of existing imported members or select 'I'm new' and type a name"
    - "A visitor can enter their email and opt in/out of deadline reminders"
    - "After signup, member sees 'Confirm with George via WhatsApp so he can approve your account' message"
    - "The landing page has Premier League-inspired branding with 'George's Predictor' name prominently displayed"
    - "Signup calls Supabase signInWithOtp with shouldCreateUser: true and correct user metadata"
    - "Blocked email addresses are rejected at signup with a clear message"
  artifacts:
    - path: "src/actions/auth.ts"
      provides: "signUpMember and requestMagicLink server actions"
      exports: ["signUpMember", "requestMagicLink"]
    - path: "src/lib/validators/auth.ts"
      provides: "Zod schemas for auth form validation"
      exports: ["signupSchema", "loginSchema"]
    - path: "src/app/(public)/page.tsx"
      provides: "Branded landing page with league table teaser"
    - path: "src/app/(public)/signup/page.tsx"
      provides: "Member signup page"
    - path: "src/components/auth/signup-form.tsx"
      provides: "Signup form component with name picker and email"
    - path: "src/components/auth/name-picker.tsx"
      provides: "Radix Select dropdown with imported names + 'I'm new' option"
  key_links:
    - from: "src/components/auth/signup-form.tsx"
      to: "src/actions/auth.ts"
      via: "form action calling signUpMember"
      pattern: "signUpMember"
    - from: "src/components/auth/name-picker.tsx"
      to: "src/lib/supabase/server.ts"
      via: "server component fetches member names"
      pattern: "createServerSupabaseClient"
    - from: "src/actions/auth.ts"
      to: "src/lib/supabase/server.ts"
      via: "server action uses Supabase client"
      pattern: "signInWithOtp"
---

<objective>
Build the member signup flow and branded landing page. Members pick their name from a dropdown of existing imported names (or select "I'm new"), enter their email, opt into deadline reminders, and receive a confirmation message directing them to contact George via WhatsApp.

Purpose: This is the member's first touchpoint with the application. It must feel professional (PL-branded), be dead simple (idiot-proof for non-technical members), and correctly create the auth.users record that triggers the Postgres function to create a pending members row.

Output: Working signup flow (landing page -> signup form -> confirmation), auth server actions, and form validation.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — types and utilities this plan depends on -->

From src/lib/supabase/types.ts:
```typescript
export type ApprovalStatus = 'pending' | 'approved' | 'rejected'
export type UserRole = 'admin' | 'member'
export interface MemberRow {
  id: string
  user_id: string
  email: string
  display_name: string
  approval_status: ApprovalStatus
  email_opt_in: boolean
  starting_points: number
  created_at: string
  approved_at: string | null
  approved_by: string | null
}
```

From src/lib/supabase/server.ts:
```typescript
export async function createServerSupabaseClient(): Promise<SupabaseClient>
```

From src/lib/supabase/admin.ts:
```typescript
export function createAdminClient(): SupabaseClient
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth server actions and validation schemas</name>
  <files>src/actions/auth.ts, src/lib/validators/auth.ts, tests/actions/auth.test.ts</files>
  <behavior>
    - signUpMember with valid name + email calls supabase.auth.signInWithOtp with shouldCreateUser: true and data: { display_name, email_opt_in }
    - signUpMember with blocked email returns { error: "This email address cannot be used for registration" }
    - signUpMember with invalid email returns validation error
    - signUpMember with empty display_name returns validation error
    - requestMagicLink with valid email calls supabase.auth.signInWithOtp with shouldCreateUser: false
    - requestMagicLink with unregistered email returns { error: "No account found with this email" }
    - Zod signupSchema validates: display_name (1-50 chars), email (valid format), is_new_member (boolean), email_opt_in (boolean)
    - Zod loginSchema validates: email (valid format)
  </behavior>
  <action>
    1. Create `src/lib/validators/auth.ts`:
       - `signupSchema`: Zod object with `display_name` (string, 1-50 chars, trimmed), `email` (string, email format, lowercase), `is_new_member` (boolean), `email_opt_in` (boolean, default true)
       - `loginSchema`: Zod object with `email` (string, email format, lowercase)
       - Export both schemas and their inferred types

    2. Create `src/actions/auth.ts`:
       - `'use server'` directive at top
       - `signUpMember(formData: FormData)`:
         a. Parse and validate with signupSchema
         b. Check blocked_emails table — if email is blocked, return `{ error: "This email address cannot be used for registration" }`
         c. Call `supabase.auth.signInWithOtp({ email, options: { shouldCreateUser: true, data: { display_name, email_opt_in } } })`
         d. Do NOT set emailRedirectTo — the initial OTP email is just a confirmation receipt, not the login mechanism. George sends the real magic link on approval.
         e. Return `{ success: true }` or `{ error: message }`
       - `requestMagicLink(formData: FormData)`:
         a. Parse and validate with loginSchema
         b. Call `supabase.auth.signInWithOtp({ email, options: { shouldCreateUser: false, emailRedirectTo: process.env.NEXT_PUBLIC_APP_URL + '/auth/callback?next=/dashboard' } })`
         c. Return `{ success: true }` or `{ error: message }`

    3. Create `tests/actions/auth.test.ts`:
       - Mock Supabase client (use helpers from tests/setup.ts)
       - Test all behaviors listed above
       - Verify signInWithOtp is called with correct parameters
       - Verify blocked email check queries blocked_emails table

    IMPORTANT per user decision: No passwords for members — magic link only. The signInWithOtp with shouldCreateUser:true is the signup mechanism. The initial OTP email serves as "registration received" confirmation — members should NOT click it to log in (George's approval magic link is the actual login).
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Georges Predictor" && npx vitest run tests/actions/auth.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - signUpMember validates input, checks blocked list, calls signInWithOtp with shouldCreateUser: true
    - requestMagicLink validates input, calls signInWithOtp with shouldCreateUser: false and correct redirectTo
    - Blocked emails are rejected with a clear user-friendly message
    - Zod schemas enforce display_name length, email format, boolean fields
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Landing page and signup form UI</name>
  <files>
    src/app/(public)/page.tsx, src/app/(public)/signup/page.tsx,
    src/app/(public)/layout.tsx, src/components/auth/signup-form.tsx,
    src/components/auth/name-picker.tsx
  </files>
  <action>
    1. Create `src/app/(public)/layout.tsx`:
       - Simple layout wrapper — no auth required
       - Centered content, dark background (Premier League aesthetic: dark navy/purple tones)
       - "George's Predictor" branding in header

    2. Create `src/app/(public)/page.tsx` — the landing page:
       - Hero section: "George's Predictor" in bold, Premier League-inspired design
       - Tagline: Something competitive and fun (e.g., "The Premier League prediction competition where bragging rights meet real money")
       - Teaser section: Placeholder league table showing top 5 positions (fetch from members table if data exists, otherwise show "Season starting soon" message)
       - Jackpot/prize mention area (per CONTEXT.md: "league table teaser, jackpot mention")
       - Two clear CTA buttons: "Join the Competition" (-> /signup) and "Member Login" (-> /login)
       - Visual style: Dark tones, bold accent colours (PL purple/green), clean modern design. NOT over the top — blend PL aesthetic with competitive energy.
       - `export const dynamic = 'force-dynamic'` (reads from DB)

    3. Create `src/components/auth/name-picker.tsx`:
       - Server component that fetches existing member display_names from the members table (where user_id IS NULL — pre-imported names without accounts)
       - Uses Radix UI Select component
       - Shows existing names as selectable options (e.g., "Big Steve", "Dan The Man")
       - Last option: "I'm new — type my name" which reveals a text input
       - If no imported names exist yet, show text input directly with note "Enter your name as it appears in the WhatsApp group"
       - Style: Large text, clear selection, mobile-friendly touch targets

    4. Create `src/components/auth/signup-form.tsx`:
       - Client component (`'use client'`)
       - Uses react-hook-form + zodResolver with signupSchema
       - Fields: Name picker (from step 3), email input, "Send me deadline reminders" checkbox (default: checked)
       - Form action calls signUpMember server action
       - Loading state on submit button
       - Error display for validation errors and server errors
       - On success: Show confirmation message — "You're registered! Confirm with George via WhatsApp so he can approve your account." (per locked decision)
       - Style: Big buttons, clear labels, no jargon, generous spacing — idiot-proof per project requirement
       - Confirmation dialogs where appropriate (per CONTEXT.md: "confirmation dialogs")

    5. Create `src/app/(public)/signup/page.tsx`:
       - Renders SignupForm within the public layout
       - Page title: "Join George's Predictor"
       - Brief instruction text above form: "Pick your name from the list below, or select 'I'm new' if this is your first season."

    IMPORTANT per locked decisions:
    - Branded landing page BEFORE login — the landing page IS the first thing visitors see
    - "Confirm with George via WhatsApp" message after signup — this exact instruction
    - Members can opt in/out of deadline reminders during signup
    - If two people claim same name, both can register — no uniqueness validation on display_name
    - ALL users are non-technical — every element must be obvious without explanation
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Georges Predictor" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Landing page renders with PL-branded design, "George's Predictor" title, league table teaser area, and two CTAs
    - Signup page has name picker dropdown (with "I'm new" option) and email input
    - Deadline reminder opt-in checkbox is present and defaults to checked
    - After successful signup, confirmation message shows "Confirm with George via WhatsApp so he can approve your account"
    - Form validates input before submission (display_name required, valid email)
    - Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx vitest run tests/actions/auth.test.ts` — all auth action tests pass
3. Landing page at `/` renders with branding
4. Signup page at `/signup` renders form with name picker and email
5. Server action correctly calls Supabase signInWithOtp
</verification>

<success_criteria>
- A member can visit the landing page, click "Join the Competition", pick their name or enter a new one, provide their email, and see the WhatsApp confirmation message
- Blocked emails are rejected at signup
- The landing page feels professional with PL-inspired branding (dark tones, bold colours)
- All form validation works (required fields, email format)
- Auth server action tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
