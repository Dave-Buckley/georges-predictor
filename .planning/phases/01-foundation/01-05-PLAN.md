---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified: []
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - AUTH-02
  - AUTH-03
  - AUTH-07

user_setup:
  - service: supabase
    why: "Database and authentication backend"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon/public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role key (keep secret)"
    dashboard_config:
      - task: "Create a new Supabase project (free tier)"
        location: "https://supabase.com/dashboard -> New Project"
      - task: "Run the migration SQL in the SQL Editor"
        location: "Supabase Dashboard -> SQL Editor -> paste contents of supabase/migrations/001_initial_schema.sql"
      - task: "Disable email confirmation for auth (members use approval flow instead)"
        location: "Supabase Dashboard -> Authentication -> Providers -> Email -> Disable 'Confirm email'"
  - service: resend
    why: "Transactional email (approval, rejection, magic links)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Create Resend account and get API key"
        location: "https://resend.com/signup"
      - task: "Verify a sending domain (or use onboarding@resend.dev for testing)"
        location: "Resend Dashboard -> Domains -> Add Domain"
  - service: vercel
    why: "Hosting and cron jobs"
    env_vars:
      - name: CRON_SECRET
        source: "Generate a random string (e.g., openssl rand -hex 32)"
    dashboard_config:
      - task: "Import project to Vercel (connect GitHub repo)"
        location: "https://vercel.com/new -> Import Git Repository"
      - task: "Set all environment variables in Vercel project settings"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables"

must_haves:
  truths:
    - "The application is accessible at a public Vercel URL"
    - "The Supabase database is active and the migration has been applied"
    - "The signup flow works end-to-end (form submission creates a pending member)"
    - "The admin login works with the seeded admin credentials"
    - "The admin can approve a pending member from the admin dashboard"
    - "The keep-alive cron job is configured and will fire daily"
    - "All external services are on free tiers (zero cost)"
  artifacts: []
  key_links:
    - from: "Vercel deployment"
      to: "Supabase project"
      via: "Environment variables"
      pattern: "NEXT_PUBLIC_SUPABASE_URL"
    - from: "vercel.json cron"
      to: "/api/keep-alive"
      via: "Daily cron schedule"
      pattern: "0 9 * * *"
---

<objective>
Deploy the application to Vercel, connect to a live Supabase database, run the migration, seed admin accounts, and verify the complete end-to-end flow works in production: signup, admin login, member approval, and session persistence.

Purpose: The application must be live and functional for George to use. This plan verifies everything works together in a real environment, not just locally.

Output: A deployed, working application at a public URL with all auth flows functional.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to Vercel and configure environment</name>
  <files></files>
  <action>
    1. Ensure .env.local exists with real values (user must have set up Supabase project and Resend account first — see user_setup).

    2. Run `npx vercel --yes` to deploy to Vercel (link to project if not already linked).

    3. Set environment variables on Vercel via CLI:
       ```
       npx vercel env add NEXT_PUBLIC_SUPABASE_URL production
       npx vercel env add NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY production
       npx vercel env add SUPABASE_SERVICE_ROLE_KEY production
       npx vercel env add CRON_SECRET production
       npx vercel env add RESEND_API_KEY production
       npx vercel env add NEXT_PUBLIC_APP_URL production
       npx vercel env add ADMIN_EMAIL_GEORGE production
       npx vercel env add ADMIN_EMAIL_DAVE production
       ```

    4. Run the database migration:
       - If Supabase CLI is available: `npx supabase db push`
       - Otherwise: Instruct user to paste `supabase/migrations/001_initial_schema.sql` into Supabase SQL Editor and run it

    5. Run the admin seed script: `npm run seed:admins`
       - This creates George and Dave's admin accounts
       - George gets a members row (he's a participant)
       - Dave gets admin-only access (no members row)

    6. Trigger a production deployment: `npx vercel --prod`

    7. Verify the deployment is live by checking the URL responds with the landing page.
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Georges Predictor" && npx vercel ls 2>&1 | head -10</automated>
  </verify>
  <done>
    - Application is deployed to Vercel at a public URL
    - All environment variables are set in Vercel project
    - Database migration has been applied to Supabase
    - Admin accounts (George + Dave) are seeded
    - Production deployment is live and responsive
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end production verification</name>
  <files></files>
  <action>
    Present the live Vercel URL to the user and guide them through verifying all auth flows, admin functionality, and infrastructure setup in production. This covers the complete Phase 1 functionality end-to-end.
  </action>
  <verify>User confirms all flows work by typing "approved"</verify>
  <done>All Phase 1 auth flows, admin features, and infrastructure verified working in production</done>
  <what-built>
    Complete foundation: Next.js application deployed on Vercel with Supabase auth, member signup (magic link), admin login (email+password), member approval/rejection flow, keep-alive cron, and full test suite.
  </what-built>
  <how-to-verify>
    Test the following flows on the live Vercel URL:

    **1. Landing Page**
    - Visit the root URL — confirm "George's Predictor" branding displays
    - Verify the page looks professional (dark PL-inspired theme, not generic)
    - Confirm "Join the Competition" and "Member Login" buttons are visible

    **2. Member Signup (AUTH-01, AUTH-09)**
    - Click "Join the Competition" to reach /signup
    - If name dropdown has imported names, select one. Otherwise, type a new name.
    - Enter a test email address
    - Check/uncheck the deadline reminder checkbox
    - Submit — confirm you see "Confirm with George via WhatsApp so he can approve your account"

    **3. Admin Login (AUTH-06, AUTH-07)**
    - Navigate to /admin/login
    - Log in with George's seeded admin credentials
    - Confirm you reach the admin dashboard
    - Verify the sidebar shows: Dashboard, Members, My Predictions, Notifications

    **4. Member Approval (ADMIN-01)**
    - Click "Members" in the sidebar
    - Confirm the test signup appears as "Pending"
    - Click "Approve" — confirm the member gets an email with a magic link
    - Verify the status changes to "Approved"

    **5. Member Login (AUTH-03, AUTH-04)**
    - Open the magic link from the approval email in a new browser/incognito
    - Confirm you land on /dashboard
    - Refresh the page — confirm you remain logged in (session persists)

    **6. Member Dashboard (AUTH-02)**
    - As approved member: confirm dashboard shows rank, fixture/result placeholders
    - Sign out and sign up a second test account but do NOT approve it
    - Log in via the second account's initial OTP link (if it works) or request magic link at /login
    - Confirm pending member sees the "waiting for approval" notice

    **7. Admin Member Management (ADMIN-06, AUTH-05)**
    - Back in admin: test "Add Member" with a manual name + email + starting points
    - Test "Edit Email" on an existing member
    - Test "Remove" on a test member (confirm confirmation dialog appears)

    **8. Keep-Alive (INFRA-03)**
    - Check Vercel dashboard: confirm the cron job for /api/keep-alive is listed
    - Optionally curl the keep-alive endpoint with the CRON_SECRET header to verify it returns 200

    **9. Free Tier Verification (INFRA-01)**
    - Confirm Supabase project is on Free tier
    - Confirm Vercel project is on Hobby (free) tier
    - Confirm Resend is on Free tier
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Application is live at a public URL
2. All auth flows work end-to-end
3. Admin dashboard is functional
4. Keep-alive cron is configured
5. All services are on free tiers
6. `npx vitest run` passes locally
</verification>

<success_criteria>
- A member can sign up, get approved by George, and log into their dashboard
- George can log in at /admin, see pending members, and approve/reject them
- Session persists across page refresh for both admin and member
- Pending members see read-only notice; approved members see full dashboard
- Keep-alive cron is configured to prevent Supabase pausing
- All infrastructure is on free tiers (zero cost)
- Human verification confirms all flows work in production
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
