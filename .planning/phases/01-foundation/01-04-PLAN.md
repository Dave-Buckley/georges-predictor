---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(public)/login/page.tsx
  - src/components/auth/login-form.tsx
  - src/app/(member)/dashboard/page.tsx
  - src/app/(member)/layout.tsx
  - src/components/member/pending-notice.tsx
  - src/components/member/dashboard-overview.tsx
  - tests/middleware.test.ts
  - tests/routing.test.ts
  - tests/api/keep-alive.test.ts
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "A member can request a magic link at /login by entering their email"
    - "After clicking the magic link, the member is authenticated and redirected to /dashboard"
    - "A pending (unapproved) member who navigates to /dashboard sees a read-only view with a notice that approval is pending"
    - "An approved member sees the full dashboard with their rank, upcoming fixtures, and deadline info"
    - "An authenticated member can refresh the page and remain logged in (session persists via cookies)"
    - "Requesting a new magic link serves as the password reset mechanism (no separate reset flow needed)"
    - "Unauthenticated users are redirected from /dashboard to /login by middleware"
  artifacts:
    - path: "src/app/(public)/login/page.tsx"
      provides: "Member magic link login page"
    - path: "src/components/auth/login-form.tsx"
      provides: "Magic link request form"
    - path: "src/app/(member)/dashboard/page.tsx"
      provides: "Member dashboard with pending/approved states"
    - path: "src/app/(member)/layout.tsx"
      provides: "Member layout with navigation"
    - path: "src/components/member/pending-notice.tsx"
      provides: "Read-only notice for unapproved members"
    - path: "tests/middleware.test.ts"
      provides: "Middleware route protection tests"
    - path: "tests/routing.test.ts"
      provides: "Route structure tests (admin vs member paths)"
    - path: "tests/api/keep-alive.test.ts"
      provides: "Keep-alive endpoint tests"
  key_links:
    - from: "src/components/auth/login-form.tsx"
      to: "src/actions/auth.ts"
      via: "form action calling requestMagicLink"
      pattern: "requestMagicLink"
    - from: "src/app/(member)/dashboard/page.tsx"
      to: "src/lib/supabase/server.ts"
      via: "server component fetches member data"
      pattern: "createServerSupabaseClient"
    - from: "src/middleware.ts"
      to: "/login"
      via: "redirect unauthenticated /dashboard access"
      pattern: "redirect.*login"
---

<objective>
Build the member login flow (magic link request), the member dashboard with pending/approved state handling, and the complete test suite covering middleware, routing, auth actions, and the keep-alive endpoint.

Purpose: This completes the member experience — they can log in via magic link, see their dashboard (with appropriate gating based on approval status), and their session persists across page refreshes. The test suite validates all auth infrastructure built across Plans 01-04.

Output: Working member login page, member dashboard with state-aware UI, and a passing test suite.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — types and utilities this plan depends on -->

From src/lib/supabase/types.ts:
```typescript
export type ApprovalStatus = 'pending' | 'approved' | 'rejected'
export interface MemberRow {
  id: string; user_id: string; email: string; display_name: string;
  approval_status: ApprovalStatus; email_opt_in: boolean;
  starting_points: number; created_at: string;
  approved_at: string | null; approved_by: string | null;
}
```

From src/lib/supabase/server.ts:
```typescript
export async function createServerSupabaseClient(): Promise<SupabaseClient>
```

From src/actions/auth.ts (Plan 02):
```typescript
export async function requestMagicLink(formData: FormData): Promise<{ success?: boolean; error?: string }>
```

From src/middleware.ts:
```typescript
// /dashboard/* requires authenticated user → redirect to /login
// /admin/* (except /admin/login) requires admin role → redirect to /admin/login
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Member login page, dashboard with approval gating, and member layout</name>
  <files>
    src/app/(public)/login/page.tsx, src/components/auth/login-form.tsx,
    src/app/(member)/layout.tsx, src/app/(member)/dashboard/page.tsx,
    src/components/member/pending-notice.tsx,
    src/components/member/dashboard-overview.tsx
  </files>
  <action>
    1. Create `src/components/auth/login-form.tsx`:
       - Client component (`'use client'`)
       - Uses react-hook-form + loginSchema (from src/lib/validators/auth.ts)
       - Single email input field + "Send me a login link" button
       - Form action calls requestMagicLink server action (from Plan 02)
       - Loading state during submission
       - On success: Show message "Check your email! We've sent you a magic link to log in." with a note "Link expires in 1 hour"
       - On error: Display user-friendly error (e.g., "No account found — have you signed up?")
       - Link to signup page: "Don't have an account? Join the competition"
       - Style: Big, clear, idiot-proof. One field, one button. No confusion.
       - NOTE: This also serves as the "password reset" mechanism per AUTH-04 — since there are no passwords, requesting a new magic link IS the recovery flow. No separate reset page needed.

    2. Create `src/app/(public)/login/page.tsx`:
       - Renders LoginForm within the public layout
       - Page title: "Member Login — George's Predictor"
       - Brief text: "Enter your email and we'll send you a link to log in."
       - No reference to passwords anywhere (members use magic links only)

    3. Create `src/app/(member)/layout.tsx`:
       - Layout for authenticated member pages
       - Fetches current user via `createServerSupabaseClient().auth.getUser()`
       - Fetches member row from members table
       - If no user: redirect to /login (defense in depth — middleware should catch this)
       - If member not found: show error state
       - Passes member data to children via a context or props
       - Simple header with "George's Predictor" branding and member's display_name
       - Navigation: Dashboard, My Predictions (placeholder), League Table (placeholder)
       - Logout button (calls supabase.auth.signOut() and redirects to /)
       - `export const dynamic = 'force-dynamic'`

    4. Create `src/components/member/pending-notice.tsx`:
       - Displayed when member's approval_status is 'pending'
       - Clear, friendly message: "Your account is waiting for George's approval. You can browse the site but can't submit predictions yet."
       - Secondary text: "If you haven't already, send George a WhatsApp message to let him know you've signed up."
       - Visual: Amber/yellow notice card, informational icon
       - Per CONTEXT.md: "Pending members get read-only access (can browse league table, fixtures) but cannot submit predictions"

    5. Create `src/components/member/dashboard-overview.tsx`:
       - Displayed when member's approval_status is 'approved'
       - Dashboard mix per CONTEXT.md:
         a. "Your Rank" card showing member's current position (calculate from starting_points for now — will use calculated points in Phase 4)
         b. "This Week's Fixtures" placeholder card (actual fixtures come in Phase 2) — show "Fixtures will appear here once the season begins"
         c. "Recent Results" placeholder card — show "Results will appear here as fixtures complete"
         d. "Upcoming Deadline" placeholder card — show "No upcoming deadlines"
       - Per CONTEXT.md: "Not just a league table, not just fixtures — a balanced overview"
       - Style: Card-based layout, responsive grid, dark PL-themed design

    6. Create `src/app/(member)/dashboard/page.tsx`:
       - Server component that reads member data from the layout context
       - If approval_status === 'pending': render PendingNotice
       - If approval_status === 'approved': render DashboardOverview
       - If approval_status === 'rejected': redirect to / (shouldn't happen — rejected members are deleted, but handle edge case)
       - `export const dynamic = 'force-dynamic'`

    IMPORTANT per locked decisions:
    - No passwords for members — magic link ONLY
    - AUTH-04 (password reset) is covered by requesting a new magic link — no separate reset flow
    - Pending members get read-only access
    - Member dashboard is a balanced overview, not just a league table
    - Session persists via @supabase/ssr cookie handling (AUTH-03) — handled by middleware token refresh
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Georges Predictor" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - /login renders magic link request form with email input
    - After magic link click, user arrives at /dashboard via auth callback
    - Pending members see the approval notice with WhatsApp reminder
    - Approved members see the dashboard overview with rank, fixtures placeholder, results placeholder
    - Member layout shows navigation with display name and logout button
    - Page refresh keeps the user logged in (session persists via SSR cookies)
    - Build succeeds
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test suite — middleware, routing, keep-alive, and integration</name>
  <files>
    tests/middleware.test.ts, tests/routing.test.ts, tests/api/keep-alive.test.ts
  </files>
  <behavior>
    Middleware tests:
    - Unauthenticated request to /dashboard returns redirect to /login
    - Authenticated non-admin request to /admin returns redirect to /admin/login
    - Authenticated admin request to /admin passes through
    - Authenticated approved member request to /dashboard passes through
    - Request to /admin/login passes through (no redirect loop)
    - Request to /login passes through (public)
    - Request to / passes through (public)
    - Request to /signup passes through (public)

    Routing tests:
    - /admin/login and /login are distinct routes (no cross-redirect)
    - /admin/* routes require admin role
    - /dashboard/* routes require authentication

    Keep-alive tests:
    - GET /api/keep-alive with valid CRON_SECRET returns 200 + { ok: true }
    - GET /api/keep-alive without auth header returns 401
    - GET /api/keep-alive with wrong secret returns 401
  </behavior>
  <action>
    1. Create `tests/middleware.test.ts`:
       - Import middleware function from src/middleware.ts
       - Create mock NextRequest objects with various URLs and cookies
       - Mock Supabase auth.getUser() to return different user states:
         a. No user (unauthenticated)
         b. User with no admin role (regular member)
         c. User with app_metadata.role = 'admin'
       - Test each route protection scenario:
         - /dashboard → redirect to /login when unauthenticated
         - /admin/members → redirect to /admin/login when not admin
         - /admin/members → pass through when admin
         - /admin/login → pass through always (avoid redirect loop)
         - / → pass through always
         - /signup → pass through always
       - Verify redirect URLs are correct

    2. Create `tests/routing.test.ts`:
       - Test that /admin/login and /login are separate routes
       - Test that the route matcher correctly identifies protected vs public paths
       - Verify middleware config matcher pattern includes the right paths
       - These are structural tests confirming the route layout is correct

    3. Create `tests/api/keep-alive.test.ts`:
       - Mock createAdminClient to return a mock Supabase client
       - Mock environment variable CRON_SECRET
       - Test valid request: Authorization header matches CRON_SECRET → 200 response with { ok: true }
       - Test missing header: → 401 response
       - Test wrong secret: → 401 response
       - Verify the mock DB query (from('members').select('id').limit(1)) is called

    4. Run the full test suite: `npx vitest run` to verify ALL tests pass (including auth tests from Plan 02 and admin tests from Plan 03).
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Georges Predictor" && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Middleware correctly redirects unauthenticated /dashboard requests to /login
    - Middleware correctly redirects non-admin /admin requests to /admin/login
    - Middleware allows admin users to access /admin routes
    - /admin/login does not redirect (no redirect loop)
    - Public routes (/, /signup, /login) are accessible without auth
    - Keep-alive route returns 200 with valid CRON_SECRET and 401 without
    - All tests across the entire suite pass: auth actions, admin actions, middleware, routing, keep-alive
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx vitest run` — ALL tests pass (full suite across all plans)
3. /login page renders magic link form
4. /dashboard renders with pending or approved state
5. Middleware correctly protects all routes
</verification>

<success_criteria>
- Members can request a magic link at /login and log in via email
- Session persists across page refreshes (cookie-based auth)
- Pending members see approval notice; approved members see dashboard overview
- Password reset is handled via magic link re-request (no separate flow)
- All middleware route protection works correctly
- Keep-alive endpoint is properly secured
- Full test suite passes with all unit and integration tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
