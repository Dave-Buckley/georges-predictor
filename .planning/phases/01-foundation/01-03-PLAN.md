---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(admin)/admin/login/page.tsx
  - src/app/(admin)/admin/layout.tsx
  - src/app/(admin)/admin/page.tsx
  - src/app/(admin)/admin/members/page.tsx
  - src/app/(admin)/admin/predictions/page.tsx
  - src/actions/admin/auth.ts
  - src/actions/admin/members.ts
  - src/components/admin/sidebar.tsx
  - src/components/admin/member-table.tsx
  - src/components/admin/member-actions.tsx
  - src/components/admin/notification-badge.tsx
  - src/lib/validators/admin.ts
  - tests/actions/admin/members.test.ts
autonomous: true
requirements:
  - AUTH-05
  - AUTH-07
  - AUTH-08
  - ADMIN-01
  - ADMIN-06

must_haves:
  truths:
    - "George and Dave can log in at /admin/login with email and password"
    - "Non-admin users are redirected away from /admin routes by middleware"
    - "Admin dashboard shows pending approvals count and action items"
    - "George can approve a pending member — which updates approval_status and sends a magic link email"
    - "George can reject a pending member — which deletes the account and sends a rejection email"
    - "George can optionally block a rejected email to prevent re-registration"
    - "George can add a new member manually with display name, email, and starting points"
    - "George can change a member's email address"
    - "George can remove a member"
    - "Admin panel has a 'My Predictions' tab (placeholder for Phase 3)"
    - "George has a members row with approval_status 'approved' (he is both admin and participant)"
  artifacts:
    - path: "src/app/(admin)/admin/login/page.tsx"
      provides: "Admin email+password login page"
    - path: "src/app/(admin)/admin/layout.tsx"
      provides: "Admin layout with sidebar navigation"
    - path: "src/app/(admin)/admin/page.tsx"
      provides: "Admin dashboard — action-focused landing page"
    - path: "src/app/(admin)/admin/members/page.tsx"
      provides: "Member management page with table and actions"
    - path: "src/actions/admin/members.ts"
      provides: "Server actions for member CRUD: approve, reject, add, remove, updateEmail, setStartingPoints"
      exports: ["approveMember", "rejectMember", "addMember", "removeMember", "updateMemberEmail", "setMemberStartingPoints"]
    - path: "src/actions/admin/auth.ts"
      provides: "Admin login server action"
      exports: ["adminLogin"]
    - path: "src/components/admin/sidebar.tsx"
      provides: "Admin sidebar navigation component"
    - path: "src/components/admin/member-table.tsx"
      provides: "Member list table with status badges"
  key_links:
    - from: "src/app/(admin)/admin/login/page.tsx"
      to: "src/actions/admin/auth.ts"
      via: "form action calling adminLogin"
      pattern: "adminLogin"
    - from: "src/app/(admin)/admin/members/page.tsx"
      to: "src/actions/admin/members.ts"
      via: "approve/reject/add/remove actions"
      pattern: "approveMember|rejectMember|addMember"
    - from: "src/actions/admin/members.ts"
      to: "src/lib/supabase/admin.ts"
      via: "admin client for user management"
      pattern: "createAdminClient"
---

<objective>
Build the admin authentication flow (email+password login at /admin/login) and the full member management system. George can approve, reject, add, remove, and edit members from a clean admin dashboard with sidebar navigation and notification badges.

Purpose: George's admin panel is the control center. Without it, he can't approve members, manage the competition, or maintain control. This is his primary interface for running the competition.

Output: Working admin login, admin dashboard shell with sidebar, and complete member management CRUD with approval/rejection email notifications.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — types and utilities this plan depends on -->

From src/lib/supabase/types.ts:
```typescript
export type ApprovalStatus = 'pending' | 'approved' | 'rejected'
export type UserRole = 'admin' | 'member'
export interface MemberRow {
  id: string
  user_id: string
  email: string
  display_name: string
  approval_status: ApprovalStatus
  email_opt_in: boolean
  starting_points: number
  created_at: string
  approved_at: string | null
  approved_by: string | null
}
```

From src/lib/supabase/server.ts:
```typescript
export async function createServerSupabaseClient(): Promise<SupabaseClient>
```

From src/lib/supabase/admin.ts:
```typescript
export function createAdminClient(): SupabaseClient
```

From src/middleware.ts:
```typescript
// /admin/* (except /admin/login) requires app_metadata.role === 'admin'
// Redirects to /admin/login if not admin
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin auth action, member management actions, and validation</name>
  <files>
    src/actions/admin/auth.ts, src/actions/admin/members.ts,
    src/lib/validators/admin.ts, tests/actions/admin/members.test.ts
  </files>
  <behavior>
    - adminLogin with valid admin email+password calls supabase.auth.signInWithPassword and redirects to /admin
    - adminLogin with non-admin email returns { error: "Invalid admin credentials" }
    - approveMember calls auth.admin.inviteUserByEmail with redirectTo to /auth/callback, updates approval_status to 'approved', sets approved_at and approved_by
    - rejectMember calls auth.admin.deleteUser, sends rejection email via Resend, optionally inserts into blocked_emails
    - addMember creates auth user via auth.admin.createUser, creates members row with custom starting_points and approval_status 'approved'
    - removeMember calls auth.admin.deleteUser (cascades to members via FK)
    - updateMemberEmail updates email in both auth.users and members table
    - setMemberStartingPoints updates starting_points column in members table
    - All admin actions verify the caller is an admin before proceeding
  </behavior>
  <action>
    1. Create `src/lib/validators/admin.ts`:
       - `adminLoginSchema`: Zod object with `email` (string, email), `password` (string, min 6)
       - `addMemberSchema`: Zod object with `display_name` (string, 1-50 chars), `email` (string, email), `starting_points` (number, min 0, default 0)
       - `updateEmailSchema`: Zod object with `member_id` (string, uuid), `new_email` (string, email)
       - Export all schemas and inferred types

    2. Create `src/actions/admin/auth.ts`:
       - `'use server'` directive
       - `adminLogin(formData: FormData)`:
         a. Validate with adminLoginSchema
         b. Check email is one of ADMIN_EMAIL_GEORGE or ADMIN_EMAIL_DAVE (hardcoded check as secondary guard per research)
         c. Call `supabase.auth.signInWithPassword({ email, password })`
         d. On success, redirect to `/admin`
         e. On failure, return `{ error: "Invalid admin credentials" }`

    3. Create `src/actions/admin/members.ts`:
       - `'use server'` directive
       - All actions use `createAdminClient()` (service_role) for user management
       - All actions verify the current user is admin before proceeding (call createServerSupabaseClient().auth.getUser() and check app_metadata.role)

       - `approveMember(memberId: string)`:
         a. Fetch member from members table
         b. Call `supabaseAdmin.auth.admin.inviteUserByEmail(email, { redirectTo: APP_URL + '/auth/callback?next=/dashboard' })`
         c. Update members: `approval_status = 'approved'`, `approved_at = now()`, `approved_by = currentAdminId`
         d. Return `{ success: true }`

       - `rejectMember(memberId: string, blockEmail: boolean)`:
         a. Fetch member to get user_id and email
         b. Delete auth user: `supabaseAdmin.auth.admin.deleteUser(userId)`
         c. Send rejection email via Resend (from: "George's Predictor", subject: "Registration Update", body: friendly message explaining their registration was not approved)
         d. If blockEmail: insert into blocked_emails table
         e. Return `{ success: true }`

       - `addMember(formData: FormData)`:
         a. Validate with addMemberSchema
         b. Create auth user: `supabaseAdmin.auth.admin.createUser({ email, email_confirm: true })`
         c. Update members row (created by trigger): set `approval_status = 'approved'`, `starting_points`, `display_name`
         d. Send welcome magic link via `supabaseAdmin.auth.admin.inviteUserByEmail(email, { redirectTo: ... })`
         e. Return `{ success: true }`

       - `removeMember(memberId: string)`:
         a. Fetch member to get user_id
         b. Delete auth user: `supabaseAdmin.auth.admin.deleteUser(userId)` — cascades via FK
         c. Return `{ success: true }`

       - `updateMemberEmail(memberId: string, newEmail: string)`:
         a. Validate email format
         b. Fetch member to get user_id
         c. Update auth user: `supabaseAdmin.auth.admin.updateUserById(userId, { email: newEmail })`
         d. Update members table: `email = newEmail`
         e. Return `{ success: true }`

       - `setMemberStartingPoints(memberId: string, points: number)`:
         a. Validate points >= 0
         b. Update members table: `starting_points = points`
         c. Return `{ success: true }`

    4. Create `tests/actions/admin/members.test.ts`:
       - Mock Supabase admin client and Resend
       - Test approveMember: calls inviteUserByEmail, updates approval_status
       - Test rejectMember: calls deleteUser, sends Resend email, optionally blocks email
       - Test addMember: creates user, updates member row
       - Test removeMember: calls deleteUser
       - Test admin auth guard: non-admin caller is rejected
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Georges Predictor" && npx vitest run tests/actions/admin/members.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Admin login action authenticates with signInWithPassword and validates against known admin emails
    - approveMember sends magic link and updates approval_status
    - rejectMember deletes user, sends rejection email, optionally blocks email
    - addMember creates a new user with starting points (for late joiners)
    - removeMember deletes user (FK cascade handles members row)
    - updateMemberEmail updates both auth.users and members table
    - All admin actions check caller is admin
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin login page, dashboard layout with sidebar, and member management UI</name>
  <files>
    src/app/(admin)/admin/login/page.tsx,
    src/app/(admin)/admin/layout.tsx,
    src/app/(admin)/admin/page.tsx,
    src/app/(admin)/admin/members/page.tsx,
    src/app/(admin)/admin/predictions/page.tsx,
    src/components/admin/sidebar.tsx,
    src/components/admin/member-table.tsx,
    src/components/admin/member-actions.tsx,
    src/components/admin/notification-badge.tsx
  </files>
  <action>
    1. Create `src/app/(admin)/admin/login/page.tsx`:
       - Email + password login form (NOT magic link — per locked decision)
       - Uses react-hook-form + adminLoginSchema
       - Form action calls adminLogin server action
       - Error display for invalid credentials
       - Style: Clean, minimal, professional. "George's Predictor — Admin" branding.
       - No link to member signup (admin login is separate per locked decision)

    2. Create `src/components/admin/sidebar.tsx`:
       - Sidebar navigation component for admin layout
       - Links: Dashboard (home), Members, My Predictions, Notifications
       - "Gameweeks", "Bonuses", "Reports" links present but disabled/greyed with "(Coming soon)" — these are Phase 2+ features
       - Active link highlighting
       - "George's Predictor" branding at top of sidebar
       - Collapsible on mobile (hamburger menu)
       - Per CONTEXT.md: "Sidebar navigation for deeper sections: Members, Gameweeks, Bonuses, Reports"

    3. Create `src/components/admin/notification-badge.tsx`:
       - Fetches unread count from admin_notifications table
       - Displays red badge with count on the Notifications sidebar link
       - Per CONTEXT.md: notifications are a to-do list feel

    4. Create `src/app/(admin)/admin/layout.tsx`:
       - Admin layout wrapper with sidebar
       - Fetches current admin user and verifies role (defense in depth — middleware already checks)
       - Passes admin user info to sidebar for display
       - `export const dynamic = 'force-dynamic'` (authenticated page)
       - Dark theme consistent with PL branding

    5. Create `src/app/(admin)/admin/page.tsx` — the admin dashboard:
       - Action-focused landing page showing what needs George's attention NOW:
         a. Pending approvals count with "Review" button (links to /admin/members?filter=pending)
         b. Recent notifications list (from admin_notifications table)
         c. Quick stats: total members, approved, pending
       - Per CONTEXT.md: "shows what needs George's attention right now (pending approvals, unconfirmed bonuses, gameweek status)"
       - Bonuses and gameweek sections show "Not yet available" placeholders (Phase 5+)
       - `export const dynamic = 'force-dynamic'`

    6. Create `src/components/admin/member-table.tsx`:
       - Table component showing all members
       - Columns: Name, Email, Status (badge: pending/approved/rejected), Starting Points, Joined date
       - Filter tabs at top: All | Pending | Approved
       - Sort by name or date
       - Status badges: pending = amber, approved = green, rejected = red
       - Style: Clean table, readable on desktop. George is not technical — big clear status badges, no ambiguity.

    7. Create `src/components/admin/member-actions.tsx`:
       - Action buttons/menu for each member row:
         a. Pending members: "Approve" (green) and "Reject" (red) buttons
         b. For Reject: Confirmation dialog (Radix Dialog) asking "Reject and block this email?" with Yes/No + block checkbox
         c. Approved members: "Edit Email", "Set Starting Points", "Remove" actions in a dropdown menu
         d. For Remove: Confirmation dialog "Are you sure? This will permanently remove {name} from the competition."
       - All actions call the corresponding server actions from Task 1
       - Per CONTEXT.md: "confirmation dialogs" and "George is not technical — prioritize simplicity"
       - "Add Member" button at the top of the page → opens dialog with display_name, email, starting_points fields

    8. Create `src/app/(admin)/admin/members/page.tsx`:
       - Fetches all members using admin Supabase client
       - Renders MemberTable + MemberActions
       - Supports URL query `?filter=pending` to pre-filter
       - Page title: "Manage Members"
       - `export const dynamic = 'force-dynamic'`

    9. Create `src/app/(admin)/admin/predictions/page.tsx`:
       - Placeholder page for George's own predictions
       - Shows message: "Predictions will be available once fixtures are loaded (coming in the next update)"
       - This satisfies AUTH-08 structurally — the tab exists, George has a members row, predictions come in Phase 3
       - Per CONTEXT.md: "Dedicated 'My Predictions' tab — George submits his own predictions here, clearly separated from admin functions"

    CRITICAL per locked decisions:
    - Separate admin login page at /admin — email + password (NOT magic link)
    - Only George and Dave's emails work for admin login
    - Action-focused landing page showing what needs attention
    - George is not technical — every admin action must be obvious with no ambiguity
    - Confirmation dialogs on destructive actions (reject, remove)
    - "My Predictions" tab exists in admin panel (placeholder for Phase 3)
  </action>
  <verify>
    <automated>cd "C:/Users/David/AI Projects/GSD Sessions/Georges Predictor" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - /admin/login page renders with email+password form
    - /admin dashboard shows pending approvals count and notification badges
    - /admin/members shows member table with filter tabs (All/Pending/Approved)
    - Approve button on pending member triggers approveMember action
    - Reject button shows confirmation dialog with block option, triggers rejectMember action
    - "Add Member" dialog allows creating members with starting points
    - Edit Email and Remove actions work with confirmation dialogs
    - /admin/predictions shows placeholder message
    - Sidebar has all navigation links with correct active states
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx vitest run tests/actions/admin/members.test.ts` — all admin action tests pass
3. /admin/login renders login form
4. /admin/ renders dashboard with pending count
5. /admin/members renders member table with action buttons
6. /admin/predictions renders placeholder
</verification>

<success_criteria>
- George can log in at /admin/login with email+password and reach the admin dashboard
- Admin dashboard shows pending approvals count and recent notifications
- George can approve a member (sends magic link, updates status)
- George can reject a member with optional email blocking
- George can add a member manually with starting points (for late joiners)
- George can edit a member's email and remove members
- My Predictions tab exists as placeholder
- All actions have confirmation dialogs on destructive operations
- Admin action tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
